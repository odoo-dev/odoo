<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="subcontracting_portal_stock_move_kanban_view" model="ir.ui.view">
        <field name="name">subcontracting.portal.stock.move.kanban</field>
        <field name="model">stock.move</field>
        <field name="priority">1000</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <field name="product_id"/>
                <field name="show_details_visible"/>
                <field name="description_picking"/>
                <field name="date"/>
                <field name="date_deadline"/>
                <field name="product_uom_qty"/>
                <field name="quantity"/>
                <field name="uom_id"/>
                <field name="show_subcontracting_details_visible"/>
                <templates>
                    <t t-name="card" class="row g-0 ps-0 pe-0">
                        <main class="col">
                            <field name="product_id" class="fw-bold"/>
                            <div class="text-muted" invisible="not description_picking">
                                <field name="description_picking"/>
                            </div>
                            <div class="d-flex justify-content-between align-items-baseline gap-2 text-muted">
                                <span>Demand:</span>
                                <span class="text-nowrap">
                                    <field name="product_uom_qty"/>
                                    <field name="uom_id" groups="uom.group_uom" widget="many2one_uom" class="ms-1"/>
                                </span>
                            </div>
                            <div class="d-flex justify-content-between align-items-baseline gap-2 text-muted">
                                <span>Done:</span>
                                <span class="text-nowrap">
                                    <field name="quantity"/>
                                    <field name="uom_id" groups="uom.group_uom" widget="many2one_uom" class="ms-1"/>
                                </span>
                            </div>
                            <div class="text-muted" invisible="not date">
                                Scheduled Date:
                                <field name="date"/>
                            </div>
                            <div class="text-muted" invisible="not date_deadline">
                                Deadline:
                                <field name="date_deadline"/>
                            </div>
                            <footer class="d-flex gap-2 mt-2">
                                <button name="action_show_details" type="object" icon="fa-list"
                                    class="btn btn-secondary" string="Details"
                                    invisible="not show_details_visible"
                                    context="{'is_subcontracting_portal': 1}"/>
                                <button name="action_show_subcontract_details" type="object" icon="fa-sitemap"
                                    class="btn btn-secondary" string="Register Components"
                                    invisible="not show_subcontracting_details_visible"/>
                            </footer>
                        </main>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="subcontracting_portal_production_form_view" model="ir.ui.view">
        <field name="name">subcontracting.portal.production.view.form</field>
        <field name="model">stock.picking</field>
        <field name="priority">999</field>
        <field name="arch" type="xml">
            <form js_class="subcontracting_portal_picking_form_view" string="Manufacturing Orders">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Manufacturing Reference" nolabel="1"/>
                        </h1>
                    </div>
                    <group>
                        <field name="state" invisible='1'/>
                        <field name="scheduled_date" readonly="state in ['cancel', 'done']" required="id" decoration-warning="state not in ('done', 'cancel') and scheduled_date &lt; now" decoration-danger="state not in ('done', 'cancel') and scheduled_date &lt; current_date" decoration-bf="state not in ('done', 'cancel') and (scheduled_date &lt; current_date or scheduled_date &lt; now)"/>
                        <field name="date_deadline" invisible="state in ('done', 'cancel') or not date_deadline" decoration-danger="date_deadline and date_deadline &lt; current_date" decoration-bf="date_deadline and date_deadline &lt; current_date"/>
                        <field name="origin" placeholder="e.g. PO0032" readonly="state in ['cancel', 'done']"/>
                    </group>
                    <notebook>
                        <page string="Operations" name="operations">
                            <field name="move_ids" mode="list,kanban" context="{'kanban_view_ref': 'mrp_subcontracting.subcontracting_portal_stock_move_kanban_view', 'default_picking_id': id, 'default_picking_type_id': picking_type_id, 'default_company_id': company_id, 'default_date': scheduled_date}">
                                <list no_open="1">
                                    <field name="id" readonly="1" column_invisible="True"/>
                                    <field name="product_id" readonly="1"/>
                                    <field name="show_details_visible" column_invisible="True"/>
                                    <field name="description_picking" string="Description" optional="hide"/>
                                    <field name="date" optional="hide"/>
                                    <field name="date_deadline" optional="hide"/>
                                    <field name="product_uom_qty" string="Demand" readonly="1"/>
                                    <field name="product_qty" readonly="1" column_invisible="True"/>
                                    <field name="quantity" string="Done"/>
                                    <field name="uom_id" groups="uom.group_uom" widget="many2one_uom"/>
                                    <button name="action_show_details" type="object" icon="format_list_bulleted" title="Details"
                                                invisible="not show_details_visible" context="{'is_subcontracting_portal': 1}"/>
                                    <field name="show_subcontracting_details_visible" column_invisible="True"/>
                                    <button name="action_show_subcontract_details" string="Register components for subcontracted product" type="object" icon="account_tree"
                                        invisible="not show_subcontracting_details_visible"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="subcontracting_portal_view_production_action" model="ir.actions.act_window">
        <field name="name">Subcontracting Portal</field>
        <field name="res_model">stock.picking</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="subcontracting_portal_production_form_view"/>
    </record>

</odoo>
