# This directory holds the generated mdi_subset.woff2 font file.
#
# The file is produced by the generate_mdi CLI command:
#
#   python odoo-bin --addons-path=addons generate_mdi \
#       --font /path/to/materialdesignicons-webfont.ttf \
#       --mapping /path/to/mdi-meta.json
#
# The generated .woff2 file is intentionally excluded from version control
# (add "*.woff2" to .gitignore if needed).  Check in the file only if you
# want to ship a pre-built font with the module.

