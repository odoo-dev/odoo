<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_wip_accounting_form" model="ir.ui.view">
            <field name="name">Post WIP Accounting Entry</field>
            <field name="model">mrp.account.wip.accounting</field>
            <field name="arch" type="xml">
                <form string="Post Manufacturing WIP">
                    <group>
                        <group>
                            <field name="journal_id"/>
                            <field name="reference"/>
                        </group>
                        <group>
                            <field name="date"/>
                            <field name="reversal_date"/>
                        </group>
                    </group>
                    <field name="mo_ids" invisible="1"/>
                    <field name="line_ids" mode="list,kanban">
                        <list editable="bottom">
                            <field name="account_id"/>
                            <field name="label"/>
                            <field name="debit" sum="Total Debit"/>
                            <field name="credit" sum="Total Credit"/>
                            <field name="currency_id" column_invisible="True"/>
                        </list>
                        <kanban class="o_kanban_mobile">
                            <field name="account_id"/>
                            <field name="label"/>
                            <field name="debit"/>
                            <field name="credit"/>
                            <field name="currency_id"/>
                            <templates>
                                <t t-name="card" class="row g-0 ps-0 pe-0">
                                    <main class="col">
                                        <field name="account_id" class="fw-bold"/>
                                        <div class="text-muted" invisible="not label">
                                            <field name="label"/>
                                        </div>
                                        <div class="d-flex justify-content-between align-items-baseline gap-2 mt-2">
                                            <span class="text-muted">Debit</span>
                                            <field name="debit" widget="monetary" options="{'currency_field': 'currency_id'}" class="fw-bold text-nowrap"/>
                                        </div>
                                        <div class="d-flex justify-content-between align-items-baseline gap-2">
                                            <span class="text-muted">Credit</span>
                                            <field name="credit" widget="monetary" options="{'currency_field': 'currency_id'}" class="fw-bold text-nowrap"/>
                                        </div>
                                    </main>
                                </t>
                            </templates>
                        </kanban>
                    </field>
                    <footer>
                        <button name="confirm" string="Post WIP" data-hotkey="q" colspan="1" type="object" class="btn-primary"/>
                        <button class="btn-secondary" special="cancel" data-hotkey="x"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_wip_accounting" model="ir.actions.act_window">
            <field name="name">Post WIP Accounting Entry</field>
            <field name="res_model">mrp.account.wip.accounting</field>
            <field name="view_mode">form</field>
            <field name="binding_model_id" ref="mrp.model_mrp_production"/>
            <field name="group_ids" eval="[(4, ref('account.group_account_user'))]"/>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
