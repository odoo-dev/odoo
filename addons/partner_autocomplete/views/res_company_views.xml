<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_company_form_inherit_partner_autocomplete" model="ir.ui.view">
        <field name="name">res.company.form.inherit.web.partner.autocomplete</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='vat']" position="after">
                <field name="iap_enrich_auto_done" invisible="1"/> <!-- loaded so saving the form doesn't reset the IAP enrich-done flag read by ir_http session_info -->
            </xpath>
        </field>
    </record>
</odoo>
