<odoo>
    <template id="address_details" inherit_id="portal.address_details">
        <select name="invoice_sending_method" position="after">
            <div t-if="peppol_eas_list" class="col-xl-6 portal_peppol_toggle">
                <label class="col-form-label label-optional" for="peppol_eas">Peppol e-Address (EAS)</label>
                <select name="peppol_eas">
                    <t t-foreach="peppol_eas_list" t-as="eas">
                        <option t-att-value="eas" t-att-selected="(peppol_eas or partner_sudo.peppol_eas) == eas">
                            <t t-out="eas_value"/>
                        </option>
                    </t>
                </select>
                <label class="col-form-label label-optional" for="peppol_endpoint">Peppol Endpoint</label>
                <input
                    type="text"
                    name="peppol_endpoint"
                    t-att-value="peppol_endpoint or partner_sudo.peppol_endpoint"
                />
            </div>
        </select>
    </template>
</odoo>
