<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="report_saleorder_document_connect_software" inherit_id="sale.report_saleorder_document">
        <xpath expr="//div[hasclass('page')]" position="inside">
            <t t-set="base_address" t-value="doc.env['ir.config_parameter'].sudo().get_param('web.base.url')"/>
            <t t-set="portal_url" t-value="base_address + '/my/orders/' + str(doc.id) + '#sale_portal_connect_software_modal'"/>
            <div t-if="any(u._is_portal() for u in doc.partner_id.user_ids)" class="text-center">
                <a t-att-href="portal_url">Connect your software</a> with <t t-out="doc.company_id.name"/> to create quotes automatically.
            </div>
        </xpath>
    </template>
</odoo>
