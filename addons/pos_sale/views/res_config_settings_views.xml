<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="pos_config_view_form_inherit_pos_sale" model="ir.ui.view">
        <field name="name">pos.config.form.view.inherit.pos_sale</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='products_pricing']/div" position="inside">
                <setting string="Sales Team" help="Sales are reported to the following sales team">
                    <field name="crm_team_id" context="{'kanban_view_ref': 'sales_team.crm_team_view_kanban'}" domain="['|', ('company_id', '=', company_id), ('company_id', '=', False)]"/>
                </setting>
                <setting id="down_payment_product" string="Down Payment Product" help="This product will be applied when down payment is made">
                    <field
                        name="down_payment_product_id"
                        domain="[('type', '=', 'service'), '|', ('company_id', '=', company_id), ('company_id', '=', False)]"
                        context="{
                            'default_type': 'service',
                            'default_taxes_id': False,
                        }"
                    />
                </setting>
                <setting id="default_product" string="Default Sale Product" help="This product will be applied on description only Sale order lines.">
                    <field
                        name="default_product_id"
                        domain="[('type', '=', 'service'), '|', ('company_id', '=', company_id), ('company_id', '=', False)]"
                        context="{
                            'default_type': 'service',
                            'default_taxes_id': False,
                        }"
                    />
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
