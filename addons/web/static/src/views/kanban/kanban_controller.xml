<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.KanbanView">
        <div t-attf-class="{{ className }} {{ hasSelectedRecords ? 'o_kanban_selection_active' : 'o_kanban_selection_available' }}" t-ref="root">
            <Layout className="model.useSampleModel ? 'o_view_sample_data' : ''" display="props.display">
                <t t-set-slot="control-panel-buttons">
                    <t t-call="{{ props.buttonTemplate }}"/>
                    <t t-if="env.isSmall and headerAlwaysButtons.length">
                        <MultiRecordViewButton t-props="multiRecordViewButtonProps(headerAlwaysButtons[0])"/>
                        <Dropdown t-if="headerAlwaysButtons.length > 1">
                            <button class="btn btn-secondary" title="More">
                                <i class="fa fa-fw fa-ellipsis-v" />
                            </button>
                            <t t-set-slot="content">
                                <t t-foreach="headerAlwaysButtons.slice(1)" t-as="button" t-key="button.id" t-if="!evalViewModifier(button.invisible)">
                                    <DropdownItem class="'o-dropdown-item-unstyled-button'">
                                        <MultiRecordViewButton t-props="multiRecordViewButtonProps(button)"/>
                                    </DropdownItem>
                                </t>
                            </t>
                        </Dropdown>
                    </t>
                    <t t-else="">
                        <t t-foreach="headerAlwaysButtons" t-as="button" t-key="button.id" t-if="!evalViewModifier(button.invisible)">
                            <MultiRecordViewButton t-props="multiRecordViewButtonProps(button)"/>
                        </t>
                    </t>
                </t>
                <t t-set-slot="control-panel-additional-actions">
                    <CogMenu t-if="!hasSelectedRecords"/>
                    <CogMenu t-elif="env.isSmall and (props.info.actionMenus or headerOtherButtons.length)" t-props="actionMenuProps" hasSelectedRecords="hasSelectedRecords">
                        <t t-foreach="headerOtherButtons" t-as="button" t-key="button.id" t-if="!evalViewModifier(button.invisible)">
                            <DropdownItem class="'o-dropdown-item-unstyled-button'">
                                <MultiRecordViewButton t-props="multiRecordViewButtonProps(button)"/>
                            </DropdownItem>
                        </t>
                    </CogMenu>
                </t>
                <t t-set-slot="control-panel-actions">
                    <div t-if="hasSelectedRecords" class="o_selection_container d-flex gap-1">
                        <SelectionBox root="this.model.root"/>
                        <t t-if="!env.isSmall">
                            <t t-foreach="headerOtherButtons" t-as="button" t-key="button.id" t-if="!evalViewModifier(button.invisible)">
                                <MultiRecordViewButton t-props="multiRecordViewButtonProps(button)"/>
                            </t>
                            <t t-if="props.info.actionMenus">
                                <ActionMenus t-props="actionMenuProps"/>
                            </t>
                        </t>
                    </div>
                    <t t-else="">
                        <SearchBar toggler="searchBarToggler" autofocus="firstLoad"/>
                    </t>
                </t>
                <t t-set-slot="control-panel-navigation-additional">
                    <t t-if="!hasSelectedRecords" t-component="searchBarToggler.component" t-props="searchBarToggler.props"/>
                </t>
                <t t-component="props.Renderer" t-if="model.isReady"
                    list="model.root"
                    archInfo="props.archInfo"
                    Compiler="props.Compiler"
                    readonly="props.readonly"
                    forceGlobalClick="props.forceGlobalClick"
                    deleteRecord.bind="deleteRecord"
                    openRecord.bind="openRecord"
                    noContentHelp="props.info.noContentHelp"
                    scrollTop.bind="scrollTop"
                    canQuickCreate="canQuickCreate"
                    quickCreateState="quickCreateState"
                    progressBarState="progressBarState"
                />
            </Layout>
        </div>
    </t>

    <t t-name="web.KanbanView.Buttons">
        <button t-if="canCreate and props.showButtons" type="button" t-att-disabled="isNewButtonDisabled" class="btn btn-primary o-kanban-button-new" accesskey="c" t-on-click="() => this.createRecord()" data-bounce-button="">
            New
        </button>
    </t>

</templates>
