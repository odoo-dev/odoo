<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.BinaryField">
        <t t-if="!props.readonly">
            <t t-if="props.record.data[props.name]">
                <div class="w-100 d-inline-flex gap-1 o_input_box cursor-pointer">
                    <FileUploader
                        acceptedFileExtensions="props.acceptedFileExtensions"
                        allowedMIMETypes="props.allowedMIMETypes"
                        onUploaded.bind="update"
                    >
                        <t name="download" t-if="props.record.resId and !props.record.dirty">
                            <button
                                class="btn btn-link fa fa-download o_download_file_button o_input_box_overlay suffix"
                                data-tooltip="Download"
                                aria-label="Download"
                                t-on-click="onFileDownload"
                            />
                        </t>
                        <t t-set-slot="toggler">
                            <span t-if="!fileName" class="btn btn-primary o_select_file_button py-0">
                                Replace current file
                            </span>
                            <t t-else="">
                                <input type="text" class="o_input o_select_file_button" t-att-value="fileName" readonly="readonly" />
                            </t>
                        </t>
                        <button
                            class="btn btn-link fa fa-trash o_clear_file_button o_input_box_overlay suffix"
                            data-tooltip="Clear"
                            aria-label="Clear"
                            t-on-click="() => this.update({})"
                        />
                    </FileUploader>
                </div>
            </t>
            <t t-else="">
                <label class="o_select_file_button btn btn-secondary w-100 o_input">
                    <FileUploader
                        acceptedFileExtensions="props.acceptedFileExtensions"
                        allowedMIMETypes="props.allowedMIMETypes"
                        onUploaded.bind="update"
                    >
                        <t t-set-slot="toggler">
                            <div class="d-flex justify-content-between align-items-end o_input_box">
                                Upload your file
                                <i class="btn btn-link o_input_box_overlay position-static suffix fa fa-upload" />
                            </div>
                        </t>
                    </FileUploader>
                </label>
            </t>
        </t>
        <t t-elif="props.record.resId and props.record.data[props.name]">
            <a class="o_form_uri" href="#" t-on-click.prevent="onFileDownload">
                <span class="fa fa-download me-2" />
                <t t-if="fileName" t-esc="fileName" />
            </a>
        </t>
    </t>

    <t t-name="web.ListBinaryField" t-inherit="web.BinaryField">
        <xpath expr="//label[hasclass('o_select_file_button')]" position="replace">
            <label class="o_select_file_button btn btn-sm btn-link p-0">
                <FileUploader
                    acceptedFileExtensions="props.acceptedFileExtensions"
                    onUploaded.bind="update"
                >
                    <t t-set-slot="toggler">
                        <i class="fa fa-upload fa-fw"/> Upload your file
                    </t>
                </FileUploader>
            </label>
        </xpath>
    </t>

</templates>
