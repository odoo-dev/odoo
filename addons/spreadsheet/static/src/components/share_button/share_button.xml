<?xml version="1.0" encoding="utf-8"?>
<templates>

  <t t-name="spreadsheet.ShareButton">
    <Dropdown
      menuClass="'spreadsheet_share_dropdown d-flex flex-column h-auto'"
      position="'bottom-end'"
      onOpened.bind="this.onOpened"
      disabled="!this.props.model"
    >
      <button t-att-class="this.togglerClass">
        <i class="fa fa-share-alt"/>
        Share
      </button>
      <t t-set-slot="content">
        <t>
          <div class="d-flex mx-3 lh-sm">
            <div class="align-self-baseline d-flex justify-content-center align-items-center flex-shrink-0 pt-1">
              <i class="fa fa-globe fa-lg" title="Share to web"></i>
            </div>
            <div class="flex-grow-1 px-2">
              <div class="lead fw-bold">Spreadsheet published</div>
              <div>Frozen version - Anyone can view</div>
            </div>
          </div>
          <div class="mt-2 mx-3 px-2 py-1 border rounded-2">
            <div t-if="this.state.url" class="d-flex justify-content-between gap-2">
              <span class="text-truncate" t-out="this.state.url"/>
              <CopyButton className="'btn-sm p-0'" content="this.state.url" successText="this.copiedText" icon="'fa-clipboard'"/>
            </div>
            <div t-else="" class="o_loading_state d-flex align-items-center justify-content-center">
              <i class="fa fa-circle-o-notch fa-spin px-2"/><span>Generating sharing link</span>
            </div>
          </div>
        </t>
      </t>
    </Dropdown>
  </t>
</templates>
