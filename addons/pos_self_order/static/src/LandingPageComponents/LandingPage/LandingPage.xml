<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="LandingPage" owl="1">
        <div class="d-flex flex-column vh-100 justify-content-between align-items-center p-3">
            <LandingPageHeader/>
            <div class="d-flex flex-column w-75 justify-content-around">
    <!-- View Menu -->
                <button type="button" t-on-click="()=>env.navigate('/products')"
                t-attf-class="btn btn-primary btn-lg my-2">
                    View Menu
                </button>
    <!-- Custom Links -->
                <t t-foreach="selfOrder.config.custom_links" t-as="link" t-key="link.id">
                    <a t-attf-href="//{{link.url}}" t-attf-class="btn btn-lg my-2 btn-{{link.style}}">
                        <t t-esc="link.name"/>
                    </a>
                </t>
            </div>
            <LandingPageFooter/>
        </div>
    </t>
</templates>

