<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="pos_self_checkout_menu_item" model="ir.ui.view">
        <field name="name">pos.config.kanban.view.inherit.self_checkout</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_config_kanban" />
        <field name="arch" type="xml">
            <xpath expr="//div[@name='card_left']" position="inside">
                <field name="self_ordering_mode" invisible="1" />
                <button invisible="self_ordering_mode != 'checkout'"
                        class="btn btn-secondary" name="preview_self_order_app" type="object">
                    Checkout page
                </button>
            </xpath>
        </field>
    </record>
</odoo>
