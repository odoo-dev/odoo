<?xml version="1.0"?>
<odoo>
    <record id="pos_config_view_form_inherit_pos_online_payment_self_order" model="ir.ui.view">
        <field name="name">pos.config.form.view.inherit.pos_online_payment_self_order</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@id='self-payment-after']" position="after">
                <div class="content-group row" invisible="not self_ordering_mode == 'mobile'">
                    <label string="Online Payment" for="self_order_online_payment_method_id" class="col-lg-4" />
                    <field name="self_order_online_payment_method_id" placeholder="Pay at cashier if empty" />
                </div>
                <div class="d-flex flex-column align-items-start w-50" invisible="not self_ordering_mode == 'mobile' and not self_ordering_pay_after == 'each'">
                    <button name="%(point_of_sale.action_payment_methods_tree)d" icon="east" type="action" string="Payment Methods" class="btn-link" />
                </div>
            </xpath>
        </field>
    </record>
</odoo>
