<templates>
    <t t-name="account.SectionRow">
        <li
            class="list-group-item border-0 o_section"
            t-att-data-id="this.props.section.id"
        >

            <div t-attf-class="d-flex align-items-center gap-1 cursor-pointer o_section_row {{this.selectedSection.sectionId == this.props.section.id ? 'o_selected_section' : ''}} {{this.props.section.display_type === 'line_subsection' ? 'o_treeEntry' : ''}}">
                <div class="o_section_icon d-flex align-items-center justify-content-center">
                    <button
                        t-if="this.props.section.children.length"
                        t-attf-class="fa btn p-0 border-0 bg-transparent {{this.props.section.isOpen ? 'fa-caret-down' : 'fa-caret-right'}}"
                        t-on-click="() => this.toggle(this.props.section)"
                    />
                </div>

                <input
                    t-if="this.props.state.renamingSectionId === this.props.section.id"
                    class="o_section_input"
                    type="text"
                    t-custom-model="this.props.state.newSectionName"
                    t-on-keydown="(ev) => this.env.onSectionInputKeydown(ev, null, this.props.section.id)"
                    t-on-blur="() => this.env.renameSection(this.props.section.id)"
                />

                <div
                    t-else=""
                    class="flex-grow-1 d-flex align-items-center gap-2 overflow-hidden"
                    t-on-click="() => this.env.setSelectedSection(this.props.section.id, this.selectedSection.filtered)"
                    t-att-title="this.props.section.name"
                >
                    <span
                        t-attf-class="text-truncate {{this.selectedSection.sectionId == this.props.section.id ? 'fw-bold' : ''}}"
                        t-out="this.props.section.name"
                    />
                </div>

                <div class="d-flex align-items-center ms-2 gap-2">
                    <span
                        class="text-muted"
                        t-out="this.getFormattedSubTotal(this.props.section)"
                    />

                    <SectionDropdown t-if="this.props.section.id" t-props="this.props"/>
                </div>
            </div>

            <ul
                t-if="this.props.section.isOpen and this.props.section.children.length"
                class="list-group border-0"
            >
                <t t-foreach="this.props.section.children" t-as="child" t-key="child.id">
                    <SectionRow
                        section="child"
                        state="this.props.state"
                    />
                </t>
            </ul>

            <input
                t-if="this.props.state.isAddingSection === ('subsection_' + this.props.section.id) and this.props.section.display_type === 'line_section'"
                class="o_section_input ps-3 ms-2 mt-1"
                type="text"
                t-custom-model="this.props.state.newSectionName"
                t-on-keydown="(ev) => this.env.onSectionInputKeydown(ev, this.props.section.id)"
                t-on-blur="() => this.env.createSection(this.props.section.id)"
            />

        </li>
    </t>
</templates>
