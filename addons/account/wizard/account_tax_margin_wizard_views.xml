<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_move_tax_margin_wizard_form" model="ir.ui.view">
        <field name="name">account.move.tax.margin.wizard.form</field>
        <field name="model">account.move.tax.margin.wizard</field>
        <field name="arch" type="xml">
            <form string="Compute Tax on Margin">
                <p class="text-muted">
                    Review the gross margin entered on the posted invoice. Complete creates one draft
                    miscellaneous entry, grouping compatible lines by their margin tax.
                </p>
                <field name="move_line_ids" nolabel="1" readonly="1">
                    <list create="0" delete="0">
                        <field name="move_id"/>
                        <field name="product_id"/>
                        <field name="quantity"/>
                        <field name="price_unit" string="Unit Price"/>
                        <field name="margin_amount" string="Gross Margin/Unit"/>
                        <field name="tax_ids" widget="many2many_tax_tags"/>
                        <field name="currency_id" column_invisible="True"/>
                    </list>
                </field>
                <footer>
                    <button string="Complete" name="action_complete" type="object" class="btn-primary"/>
                    <button string="Close" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_compute_tax_on_margin" model="ir.actions.server">
        <field name="name">Compute Tax on Margin</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="group_ids" eval="[(4, ref('account.group_account_invoice'))]"/>
        <field name="binding_model_id" eval="False"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_open_tax_on_margin_wizard()</field>
    </record>
</odoo>
