<?xml version="1.0"?>
<odoo><data>
    <record id="event_tag_view_tree" model="ir.ui.view">
        <field name="name">event.tag.view.list.inherit.online</field>
        <field name="model">event.tag</field>
        <field name="inherit_id" ref="event.event_tag_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="color" widget="color_picker"/>
            </xpath>
        </field>
    </record>

    <record id="event_tag_view_form" model="ir.ui.view">
        <field name="name">event.tag.view.form.inherit.online</field>
        <field name="model">event.tag</field>
        <field name="inherit_id" ref="event.event_tag_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="color" widget="color_picker"/>
            </xpath>
        </field>
    </record>

</data></odoo>
