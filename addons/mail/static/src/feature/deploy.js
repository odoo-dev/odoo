/** @odoo-module alias=mail.feature.deploy **/

/**
 * @param {web.env} env
 * @param {...Feature} features
 */
export default function deploy(env, ...features) {
    // TODO
    // register all items and flatten everything
}
