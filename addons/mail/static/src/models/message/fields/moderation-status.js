/** @odoo-module alias=mail.models.Message.fields.moderationStatus **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'moderationStatus',
    id: 'mail.models.Message.fields.moderationStatus',
    global: true,
});
