/** @odoo-module alias=mail.models.Message.fields.subject **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'subject',
    id: 'mail.models.Message.fields.subject',
    global: true,
});
