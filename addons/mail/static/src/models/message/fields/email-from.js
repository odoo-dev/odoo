/** @odoo-module alias=mail.models.Message.fields.emailFrom **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'emailFrom',
    id: 'mail.models.Message.fields.emailFrom',
    global: true,
});
