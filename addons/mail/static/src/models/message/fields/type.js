/** @odoo-module alias=mail.models.Message.fields.type **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'type',
    id: 'mail.models.Message.fields.type',
    global: true,
});
