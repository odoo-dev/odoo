/** @odoo-module alias=mail.models.ActivityType.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.ActivityType.fields.id',
    global: true,
    isId: true,
});
