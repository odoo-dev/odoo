/** @odoo-module alias=mail.models.Composer.fields.hasFocus **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'hasFocus',
    id: 'mail.models.Composer.fields.hasFocus',
    global: true,
    default: false,
});
