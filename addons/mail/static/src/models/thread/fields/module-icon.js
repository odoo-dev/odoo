/** @odoo-module alias=mail.models.Thread.fields.moduleIcon **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'moduleIcon',
    id: 'mail.models.Thread.fields.moduleIcon',
    global: true,
});
