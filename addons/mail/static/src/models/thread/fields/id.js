/** @odoo-module alias=mail.models.Thread.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.Thread.fields.id',
    global: true,
    isId: true,
});
