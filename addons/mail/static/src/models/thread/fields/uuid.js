/** @odoo-module alias=mail.models.Thread.fields.uuid **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'uuid',
    id: 'mail.models.Thread.fields.uuid',
    global: true,
});
