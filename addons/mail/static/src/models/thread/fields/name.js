/** @odoo-module alias=mail.models.Thread.fields.name **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'name',
    id: 'mail.models.Thread.fields.name',
    global: true,
});
