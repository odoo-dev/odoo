/** @odoo-module alias=mail.models.ChatWindowManager.fields.isHiddenMenuOpen **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'isHiddenMenuOpen',
    id: 'mail.models.ChatWindowManager.fields.isHiddenMenuOpen',
    global: true,
    default: false,
});
