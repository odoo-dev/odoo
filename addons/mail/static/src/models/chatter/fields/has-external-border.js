/** @odoo-module alias=mail.models.Chatter.fields.hasExternalBorder **/

import attr from 'mail.model.fields.attr.define';

export default attr({
    name: 'hasExternalBorder',
    id: 'mail.models.Chatter.fields.hasExternalBorder',
    global: true,
    default: true,
});
