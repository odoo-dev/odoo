/** @odoo-module alias=mail.models.Chatter.fields.context **/

import attr from 'mail.model.fields.attr.define';

export default attr({
    name: 'context',
    id: 'mail.models.Chatter.fields.context',
    global: true,
    default: {},
});
