/** @odoo-module alias=mail.models.Chatter.fields.hasTopbarCloseButton **/

import attr from 'mail.model.fields.attr.define';

export default attr({
    name: 'hasTopbarCloseButton',
    id: 'mail.models.Chatter.fields.hasTopbarCloseButton',
    global: true,
    default: false,
});
