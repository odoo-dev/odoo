/** @odoo-module alias=mail.models.Chatter.fields.isActivityBoxVisible **/

import attr from 'mail.model.fields.attr.define';

export default attr({
    name: 'isActivityBoxVisible',
    id: 'mail.models.Chatter.fields.isActivityBoxVisible',
    global: true,
    default: true,
});
