/** @odoo-module alias=mail.models.NotificationGroup.fields.date **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'date',
    id: 'mail.models.NotificationGroup.fields.date',
    global: true,
});
