/** @odoo-module alias=mail.models.NotificationGroup.fields.resId **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'resId',
    id: 'mail.models.NotificationGroup.fields.resId',
    global: true,
});
