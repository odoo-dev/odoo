/** @odoo-module alias=mail.models.NotificationGroup.fields.resModel **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'resModel',
    id: 'mail.models.NotificationGroup.fields.resModel',
    global: true,
});
