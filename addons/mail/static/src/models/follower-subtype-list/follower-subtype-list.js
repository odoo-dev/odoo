/** @odoo-module alias=mail.models.FollowerSubtypeList **/

import model from 'mail.model.define';

export default model({
    name: 'FollowerSubtypeList',
    id: 'mail.models.FollowerSubtypeList',
    global: true,
    fields: [
        'mail.models.FollowerSubtypeList.fields.follower',
    ],
});
