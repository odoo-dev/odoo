/** @odoo-module alias=mail.models.Messaging.fields.isInitialized **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'isInitialized',
    id: 'mail.models.Messaging.fields.isInitialized',
    global: true,
    default: false,
});
