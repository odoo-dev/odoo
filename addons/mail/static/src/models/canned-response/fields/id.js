/** @odoo-module alias=mail.models.CannedResponse.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.CannedResponse.fields.id',
    global: true,
    isId: true,
});
