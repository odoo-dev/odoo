/** @odoo-module alias=mail.models.Follower.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.Follower.fields.id',
    global: true,
    isId: true,
});
