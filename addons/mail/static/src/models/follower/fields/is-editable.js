/** @odoo-module alias=mail.models.Follower.fields.isEditable **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'isEditable',
    id: 'mail.models.Follower.fields.isEditable',
    global: true,
    default: false,
});
