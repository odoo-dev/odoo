/** @odoo-module alias=mail.models.Follower.fields.isActive **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'isActive',
    id: 'mail.models.Follower.fields.isActive',
    global: true,
    default: true,
});
