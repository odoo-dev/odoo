/** @odoo-module alias=mail.models.FollowerSubtype.fields.name **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'name',
    id: 'mail.models.FollowerSubtype.fields.name',
    global: true,
});
