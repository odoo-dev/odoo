/** @odoo-module alias=mail.models.FollowerSubtype.fields.resModel **/

import attr from 'mail.model.field.attr.define';

// AKU FIXME: use relation instead
export default attr({
    name: 'resModel',
    id: 'mail.models.FollowerSubtype.fields.resModel',
    global: true,
});
