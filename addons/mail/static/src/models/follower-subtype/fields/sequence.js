/** @odoo-module alias=mail.models.FollowerSubtype.fields.sequence **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'sequence',
    id: 'mail.models.FollowerSubtype.fields.sequence',
    global: true,
});
