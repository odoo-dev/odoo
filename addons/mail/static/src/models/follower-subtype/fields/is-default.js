/** @odoo-module alias=mail.models.FollowerSubtype.fields.isDefault **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'isDefault',
    id: 'mail.models.FollowerSubtype.fields.isDefault',
    global: true,
    default: false,
});
