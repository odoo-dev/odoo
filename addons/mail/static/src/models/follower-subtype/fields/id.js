/** @odoo-module alias=mail.models.FollowerSubtype.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.FollowerSubtype.fields.id',
    global: true,
    isId: true,
});
