/** @odoo-module alias=mail.models.Partner.fields.name **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'name',
    id: 'mail.models.Partner.fields.name',
    global: true,
});
