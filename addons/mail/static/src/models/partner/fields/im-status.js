/** @odoo-module alias=mail.models.Partner.fields.imStatus **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'imStatus',
    id: 'mail.models.Partner.fields.imStatus',
    global: true,
});
