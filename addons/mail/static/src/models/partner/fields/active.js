/** @odoo-module alias=mail.models.Partner.fields.active **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'active',
    id: 'mail.models.Partner.fields.active',
    global: true,
    default: true,
});
