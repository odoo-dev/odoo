/** @odoo-module alias=mail.models.Partner.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.Partner.fields.id',
    global: true,
    isId: true,
});
