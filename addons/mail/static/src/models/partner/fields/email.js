/** @odoo-module alias=mail.models.Partner.fields.email **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'email',
    id: 'mail.models.Partner.fields.email',
    global: true,
});
