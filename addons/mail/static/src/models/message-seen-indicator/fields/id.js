/** @odoo-module alias=mail.models.MessageSeenIndicator.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.MessageSeenIndicator.fields.id',
    global: true,
});
