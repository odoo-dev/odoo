/** @odoo-module alias=mail.models.Attachment.fields.filename **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'filename',
    id: 'mail.models.Attachment.fields.filename',
    global: true,
});
