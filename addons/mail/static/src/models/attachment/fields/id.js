/** @odoo-module alias=mail.models.Attachment.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.Attachment.fields.id',
    global: true,
    isId: true,
});
