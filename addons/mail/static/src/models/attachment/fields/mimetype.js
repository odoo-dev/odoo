/** @odoo-module alias=mail.models.Attachment.fields.mimetype **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'mimetype',
    id: 'mail.models.Attachment.fields.mimetype',
    global: true,
    default: '',
});
