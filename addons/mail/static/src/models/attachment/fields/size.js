/** @odoo-module alias=mail.models.Attachment.fields.size **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'size',
    id: 'mail.models.Attachment.fields.size',
    global: true,
});
