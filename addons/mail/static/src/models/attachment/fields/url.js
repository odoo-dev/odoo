/** @odoo-module alias=mail.models.Attachment.fields.url **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'url',
    id: 'mail.models.Attachment.fields.url',
    global: true,
});
