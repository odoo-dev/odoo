/** @odoo-module alias=mail.models.Attachment.fields.name **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'name',
    id: 'mail.models.Attachment.fields.name',
    global: true,
});
