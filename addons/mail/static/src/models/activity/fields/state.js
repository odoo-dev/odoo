/** @odoo-module alias=mail.models.Activity.fields.state **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'state',
    id: 'mail.models.Activity.fields.state',
    global: true,
});
