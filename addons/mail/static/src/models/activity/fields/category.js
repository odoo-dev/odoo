/** @odoo-module alias=mail.models.Activity.fields.category **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'category',
    id: 'mail.models.Activity.fields.category',
    global: true,
});
