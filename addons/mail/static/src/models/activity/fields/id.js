/** @odoo-module alias=mail.models.Activity.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.Activity.fields.id',
    global: true,
    isId: true,
});
