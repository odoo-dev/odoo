/** @odoo-module alias=mail.models.Activity.fields.summary **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'summary',
    id: 'mail.models.Activity.fields.summary',
    global: true,
});
