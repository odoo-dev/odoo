/** @odoo-module alias=mail.models.Activity.fields.icon **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'icon',
    id: 'mail.models.Activity.fields.icon',
    global: true,
});
