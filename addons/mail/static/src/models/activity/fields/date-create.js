/** @odoo-module alias=mail.models.Activity.fields.dateCreate **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'dateCreate',
    id: 'mail.models.Activity.fields.dateCreate',
    global: true,
});
