/** @odoo-module alias=mail.models.Activity.fields.dateDeadline **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'dateDeadline',
    id: 'mail.models.Activity.fields.dateDeadline',
    global: true,
});
