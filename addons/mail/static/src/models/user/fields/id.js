/** @odoo-module alias=mail.models.User.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.User.fields.id',
    global: true,
    isId: true,
});
