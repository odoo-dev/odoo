/** @odoo-module alias=mail.models.Country.fields.code **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'code',
    id: 'mail.models.Country.fields.code',
    global: true,
});
