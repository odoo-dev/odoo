/** @odoo-module alias=mail.models.Country.fields.name **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'name',
    id: 'mail.models.Country.fields.name',
    global: true,
});
