/** @odoo-module alias=mail.models.Country.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.Country.fields.id',
    global: true,
    isId: true,
});
