/** @odoo-module alias=mail.models.Notification.fields.status **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'status',
    id: 'mail.models.Notification.fields.status',
    global: true,
});
