/** @odoo-module alias=mail.models.Notification.fields.type **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'type',
    id: 'mail.models.Notification.fields.type',
    global: true,
});
