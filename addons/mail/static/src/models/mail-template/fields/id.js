/** @odoo-module alias=mail.models.MailTemplate.fields.id **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'id',
    id: 'mail.models.MailTemplate.fields.id',
    global: true,
    isId: true,
});
