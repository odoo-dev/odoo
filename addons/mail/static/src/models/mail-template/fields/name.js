/** @odoo-module alias=mail.models.MailTemplate.fields.name **/

import attr from 'mail.model.field.attr.define';

export default attr({
    name: 'name',
    id: 'mail.models.MailTemplate.fields.name',
    global: true,
});
