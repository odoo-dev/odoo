<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="discuss.ChannelMember">
        <div class="o-discuss-ChannelMember d-flex align-items-center p-1 bg-inherit rounded-3" t-att-class="attClass" t-on-click.stop="(ev) => this.onClickAvatar(ev)">
            <div class="bg-inherit o-discuss-ChannelMember-avatar position-relative d-flex flex-shrink-0 rounded-3">
                <img class="w-100 h-100 rounded-3 object-fit-cover" t-att-src="member.avatarUrl"/>
                <ImStatus member="member" className="'position-absolute top-100 start-100 translate-middle mt-n1 ' + (member.isTyping ? 'ms-n2' : 'ms-n1')" size="'md'"/>
            </div>
            <div t-ref="displayName" class="d-flex overflow-hidden flex-column lh-sm">
                <span class="ms-2 text-truncate text-muted fw-bold" t-esc="member.name" t-att-title="member.name"/>
            </div>
            <i t-if="member.channel_role === 'owner'" class="ms-2 oi oi-filled text-warning" data-icon="star" role="img" title="Channel Owner" aria-label="Channel Owner"/>
            <i t-if="member.channel_role === 'admin'" class="ms-2 oi oi-filled text-primary" data-icon="star" role="img" title="Channel Admin" aria-label="Channel Admin"/>
            <span class="ms-auto">
                <span t-if="member.in(member.channel_id.invited_member_ids)" class="p-1 oi opacity-75" data-icon="person_add"/>
                <span t-if="member.rtcSession?.is_muted and !member.rtcSession?.is_deaf" class="oi p-1 opacity-75" data-icon="mic_off"/>
                <span t-elif="member.rtcSession?.is_deaf" class="oi p-1 opacity-75" data-icon="hearing_disabled"/>
                <span t-if="member.rtcSession?.raisingHand" class="oi p-1" data-icon="back_hand"/>
            </span>
        </div>
    </t>
</templates>
