<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

<t t-name="mail.autogrow_input" owl="1">
    <input
        class="o-mail-autogrow-input p-1 lead fw-bold border"
        t-att-class="{'bg-transparent border-transparent': !inputFocus.isFocus and !inputHover.isHover}"
        t-attf-class="{{ props.className }}"
        t-att-disabled="props.disabled"
        t-att-title="state.value"
        t-model="state.value"
        t-on-keydown="onKeydownInput"
        t-ref="input"
        type="text"
    />
</t>

</templates>
