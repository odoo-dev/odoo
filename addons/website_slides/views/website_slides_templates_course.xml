<?xml version="1.0" ?>
<odoo><data>

<!-- Channels sub-template: header -->
<template id="course_nav" name="Course Navigation Header">
    <div class="o_wslides_course_nav">
        <div class="container">
            <div class="row align-items-center justify-content-between">
                <!-- Desktop Mode -->
                <nav aria-label="breadcrumb" class="col-md-8 d-none d-md-flex">
                    <ol class="breadcrumb flex-nowrap bg-transparent mb-0 ps-0 py-0 overflow-hidden">
                        <li class="breadcrumb-item flex-shrink-0">
                            <a href="/slides" title="Courses">Courses</a>
                        </li>
                        <t t-set="channel_is_active" t-value="not search_category and not search_tag and not search_slide_category and not slide"/>
                        <li t-att-class="'breadcrumb-item text-truncate' + (' active' if channel_is_active else '')" t-att-aria-current="'page' if channel_is_active else None">
                            <t t-if="channel_is_active">
                                <span class="text-truncate d-block" t-out="channel.name" t-att-title="channel.name"/>
                            </t>
                            <t t-else="">
                                <a t-if="invite_preview" t-attf-href="/slides/#{channel.id}?#{keep_query('invite_hash', 'invite_partner_id')}" class="text-truncate d-block"><span t-out="channel.name" t-att-title="channel.name"/></a>
                                <a t-else="" t-att-href="'/slides/%s' % slug(channel)" class="text-truncate d-block"><span t-out="channel.name" t-att-title="channel.name"/></a>
                            </t>
                        </li>
                        <li t-att-class="'breadcrumb-item text-truncate' + (' active' if not slide else '')" t-att-aria-current="'page' if not slide else None" t-if="search_category">
                            <t t-if="not slide">
                                <span t-out="search_category.name" t-att-title="search_category.name"/>
                            </t>
                            <t t-else="">
                                <a t-if="invite_preview" t-attf-href="/slides/#{channel.id}/category/#{search_category.id}?#{keep_query('invite_hash', 'invite_partner_id')}"><span t-out="search_category.name" t-att-title="search_category.name"/></a>
                                <a t-else="" t-attf-href="/slides/#{slug(channel)}/category/#{slug(search_category)}"><span t-out="search_category.name" t-att-title="search_category.name"/></a>
                            </t>
                        </li>
                        <li t-att-class="'breadcrumb-item text-truncate' + (' active' if not slide else '')" t-att-aria-current="'page' if not slide else None" t-if="search_tag">
                            <t t-if="not slide">
                                <span t-out="search_tag.name" t-att-title="search_tag.name"/>
                            </t>
                            <t t-else="">
                                <a t-att-href="'/slides/%s/tag/%s' % (slug(channel), slug(search_tag))"><span t-out="search_tag.name" t-att-title="search_tag.name"/></a>
                            </t>
                        </li>
                        <li t-att-class="'breadcrumb-item text-truncate' + (' active' if not slide else '')" t-att-aria-current="'page' if not slide else None" t-if="search_uncategorized">
                            <t t-if="not slide">
                                <span title="Uncategorized">Uncategorized</span>
                            </t>
                            <t t-else="">
                                <a t-if="invite_preview" t-attf-href="/slides/#{channel.id}?uncategorized=1&amp;#{keep_query('invite_hash', 'invite_partner_id')}" title="Uncategorized">Uncategorized</a>
                                <a t-else="" t-attf-href="/slides/#{slug(channel)}?uncategorized=1" title="Uncategorized">Uncategorized</a>
                            </t>
                        </li>
                        <li t-att-class="'breadcrumb-item text-truncate' + (' active' if not slide else '')" t-att-aria-current="'page' if not slide else None" t-if="search_slide_category">
                            <t t-if="not slide">
                                <span t-out="slide_categories.get('search_slide_category', '-')" t-att-title="slide_categories.get('search_slide_category', '-')"/>
                            </t>
                            <t t-else="">
                                <a t-att-href="'/slides/%s?slide_category=%s' % (slug(channel), search_slide_category)">
                                    <span t-out="slide_categories.get('search_slide_category', '-')" t-att-title="slide_categories.get('search_slide_category', '-')"/>
                                </a>
                            </t>
                        </li>
                        <li t-if="slide" class="breadcrumb-item text-truncate active" aria-current="page">
                            <span t-out="slide.name" t-att-title="slide.name"/>
                        </li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</template>


<!-- Channel main template -->
<template id='course_main' name="Course Main">
    <t t-set="head">
        <script type="module" src="/web/static/lib/pdfjs/build/pdf.js"/>
        <script type="module" src="/web/static/lib/pdfjs/build/pdf.worker.js"/>
    </t>
    <t t-set="body_classname" t-value="'o_wslides_body'"/>
    <t t-call="website.layout">
        <div id="wrap" t-attf-class="wrap mt-0">
            <div class="oe_structure oe_empty">
                <div class="o_wslides_course_header position-relative">
                    <div class="container mt-4">
                        <t t-call="website_slides.course_nav"/>
                        <t t-if="channel.image_1920"
                           t-set="_cover_bg"
                           t-value="'/web/image/slide.channel/%s/image_1920' % channel.id"/>
                        <t t-else=""
                           t-set="_cover_bg"
                           t-value="'/website_slides/static/src/img/channel-training-default.jpg'"/>
                        <div class="o_wslides_course_cover o_cc o_cc4 rounded-4 overflow-hidden mt-3 p-3 p-lg-5 pt-0"
                             t-attf-style="background-image: url(#{_cover_bg});">
                            <div class="o_wslides_course_cover_overlay"/>
                            <div class="pt128 position-relative z-1">
                                <t t-if="channel.is_member">
                                    <t t-if="channel.completed">
                                        <span class="badge rounded mb-2 bg-success"><i class="oi me-1" data-icon="check"/>Completed</span>
                                    </t>
                                    <t t-elif="channel.completion > 0">
                                        <span class="badge rounded mb-2 bg-info">In progress</span>
                                    </t>
                                    <t t-else="">
                                        <span class="badge rounded mb-2 bg-light"><i class="oi me-1" data-icon="check"/>Enrolled</span>
                                    </t>
                                </t>
                                <h1 t-field="channel.name"/>
                                <div class="d-none d-lg-block" t-field="channel.description"/>
                                <div class="d-flex flex-wrap align-items-center gap-2 mt-2 o_not_editable">
                                    <t t-if="channel.allow_comment and rating_count">
                                        <span class="d-flex align-items-center gap-1 o_wslides_header_text">
                                            <i class="oi text-warning" data-icon="star"/>
                                            <span class="text-white" t-out="'%.1f' % (rating_avg or 0.0)"/>
                                            <span class="d-none d-lg-inline text-muted">(<t t-out="rating_count"/> reviews)</span>
                                        </span>
                                        <span t-if="channel.total_time or channel.total_slides or channel.slide_category_ids" class="opacity-50">·</span>
                                    </t>
                                    <span t-if="channel.total_time" class="d-flex align-items-center gap-1">
                                        <i class="oi" data-icon="schedule"/>
                                        <span t-out="channel.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/>
                                    </span>
                                    <span t-if="channel.total_slides" class="d-flex align-items-center gap-1">
                                        <span t-if="channel.total_time" class="opacity-50">·</span>
                                        <i class="oi" data-icon="book"/>
                                        <t t-out="channel.total_slides"/> lessons
                                    </span>
                                    <span t-if="channel.slide_category_ids" class="d-flex align-items-center gap-1">
                                        <span t-if="channel.total_slides or channel.total_time" class="opacity-50">·</span>
                                        <t t-out="len(channel.slide_category_ids)"/> chapters
                                    </span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="o_wslides_course_main mt32">
                <t t-set="channel_frontend_tags" t-value="channel.tag_ids.filtered(lambda tag: tag.color)"/>
                <div class="container mb-5">
                    <div class="row">
                        <div class="col-12 col-lg-8 col-xxl-9 position-relative pe-lg-5">
                            <ul class="nav nav-underline o_wslides_nav_tabs flex-nowrap border-bottom" role="tablist" id="profile_extra_info_tablist">
                                <li class="nav-item" role="presentation">
                                    <a t-att-class="'nav-link %s' % ('active' if active_tab == 'home' else '')"
                                        id="home-tab" data-bs-toggle="pill" href="#home" role="tab" aria-controls="home"
                                        t-att-aria-selected="'true' if active_tab == 'home' else 'false'">
                                        Course
                                    </a>
                                </li>
                                <li t-if="channel.allow_comment" class="nav-item o_wslides_course_header_nav_review" role="presentation">
                                    <a t-att-class="'nav-link %s' % ('active' if active_tab == 'review' else '')"
                                        id="review-tab" data-bs-toggle="pill" href="#review" role="tab" aria-controls="review"
                                        t-att-aria-selected="'true' if active_tab == 'review' else 'false'">
                                        Reviews<t t-if="rating_count"> (<t t-out="rating_count"/>)</t>
                                    </a>
                                </li>
                            </ul>

                            <div class="tab-content py-4 o_wslides_tabs_content mb-4" id="courseMainTabContent">
                                <div t-att-class="'tab-pane fade %s' % ('show active' if active_tab == 'home' else '')" id="home" role="tabpanel" aria-labelledby="home-tab">
                                    <!-- ==== Error notification ==== -->
                                    <div t-if="access_error == 'course_content'" class="alert alert-danger alert-dismissable mb-1" role="alert">
                                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                                        You need to join this course to access "<t t-out="access_error_content_name"/>".
                                    </div>
                                    <!-- ==== Invitation Banner ==== -->
                                    <div t-if="invite_preview" class="o_wslides_identification_banner alert alert-success mb-4 p-2 px-3" role="alert">
                                        You have been invited to this course.
                                        <a class="o_underline" t-attf-href="/slides/#{channel.id}/identify?#{keep_query('invite_partner_id', 'invite_hash')}">
                                            <t t-if="is_partner_without_user">Sign up</t>
                                            <t t-else="">Log in</t>
                                        </a> to browse preview content and enroll.
                                    </div>
                                    <!-- Mobile: context info blocks rendered in content flow (sidebar hidden on mobile) -->
                                    <t t-call="website_slides.course_join_mobile_info"/>
                                    <t t-if="channel.channel_type == 'training'" t-call="website_slides.course_slides_list"/>
                                    <t t-else="" t-call="website_slides.course_slides_cards"/>
                                    <!-- Mobile: course metadata (below lesson list, hidden on desktop) -->
                                    <t t-call="website_slides.course_metadata_mobile"/>
                                </div>
                                <div t-if="channel.allow_comment" t-att-class="'tab-pane fade %s' % ('show active' if active_tab == 'review' else '')" id="review" role="tabpanel" aria-labelledby="review-tab">
                                    <t t-call="portal.message_thread"
                                        object="channel"
                                        hash="message_post_hash"
                                        pid="message_post_pid"
                                        hide_header="True"
                                        display_rating="True"
                                        disable_composer="True"
                                        two_columns="True"/>
                                </div>
                            </div>
                        </div>
                        <!-- Right Sidebar (hidden on mobile — CTA moved to fixed bottom bar) -->
                        <div class="col-12 col-lg-4 col-xxl-3 mt-3 mt-lg-0 d-none d-lg-block">
                            <t t-call="website_slides.course_sidebar"/>
                        </div>
                    </div>
                </div>
                <!-- Mobile: fixed bottom CTA bar -->
                <t t-call="website_slides.course_mobile_cta_bar"/>
            </div>
        </div>
    </t>
</template>

<template id="course_sidebar" name="Course Sidebar (infos, CTA)">
    <div class="o_wslides_course_sidebar card position-relative">
        <!-- Leave/subscribe to course -->
        <t t-if="channel.is_member">
            <div class="position-absolute top-0 end-0 p-2 z-1">
                <div class="dropdown">
                    <button class="btn btn-link text-muted lh-1 p-1"
                            type="button"
                            data-bs-toggle="dropdown"
                            data-bs-auto-close="outside"
                            aria-expanded="false"
                            aria-label="Course options">
                        <i class="oi" data-icon="more_vert"/>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end o_wslides_course_options_menu shadow-sm"
                        t-att-data-channel-id="channel.id"
                        t-att-data-enroll="channel.enroll"
                        t-att-data-visibility="channel.visibility">
                        <li class="px-3 py-2">
                            <div class="d-flex align-items-center justify-content-between gap-3">
                                <label class="form-check-label"
                                       t-attf-for="notify_toggle_#{channel.id}">
                                    Receive notifications
                                </label>
                                <div class="form-check form-switch mb-0">
                                    <input class="form-check-input o_wslides_js_notify_toggle"
                                           type="checkbox"
                                           role="switch"
                                           t-attf-id="notify_toggle_#{channel.id}"
                                           t-att-checked="'checked' if channel.message_is_follower else None"/>
                                </div>
                            </div>
                        </li>
                        <li><hr class="dropdown-divider"/></li>
                        <li>
                            <button class="dropdown-item text-danger o_wslides_js_leave_course"
                                    type="button">
                                <i class="oi me-2" data-icon="logout"/>Leave course
                            </button>
                        </li>
                    </ul>
                </div>
            </div>
        </t>
        <div class="card-body">
            <!-- Course status: enrolled member -->
            <t t-if="channel.is_member">
                <span t-attf-class="badge rounded mb-3 #{'bg-success' if channel.completed else 'bg-info' if channel.completion > 0 else 'bg-light'}">
                    <t t-if="channel.completed"><i class="oi" data-icon="check"/> Completed</t>
                    <t t-elif="channel.completion > 0">In progress</t>
                    <t t-else="">Enrolled</t>
                </span>
                <small t-if="channel.completed" class="d-block text-muted">You've completed this course. Revisit any lesson below.</small>
                <t t-elif="not channel.completed and channel.completion > 0">
                    <t t-set="time_left" t-value="channel.total_time * (1 - channel.completion / 100.0) if channel.total_time else 0"/>
                    <div class="o_wslides_channel_completion_progressbar d-flex flex-column gap-2">
                        <div class="flex-grow-1">
                            <t t-call="website_slides.course_progress_bar"/>
                        </div>
                        <t t-call="website_slides.course_progress_stats_label"/>
                    </div>
                </t>
                <small t-else="" class="d-block text-muted">Not started yet</small>
            </t>

            <!-- CTA: join / enroll button (only for non-members) -->
            <t t-call="website_slides.course_join"/>

            <!-- Metadata -->
            <div class="row row-cols-2 gy-3">
                <t t-if="channel.user_id">
                    <small class="text-muted">Responsible</small>
                    <small class="fw-medium text-end" t-out="channel.user_id.display_name"/>
                </t>
                <small class="text-muted">Last update</small>
                <small class="fw-medium text-end" t-out="channel.slide_last_update" t-options="{'widget': 'date'}"/>
                <t t-if="channel.total_time">
                    <small class="text-muted">Duration</small>
                    <small class="fw-medium text-end" t-out="channel.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/>
                </t>
                <t t-if="channel.total_slides">
                    <small class="text-muted">Lessons</small>
                    <small class="fw-medium text-end" t-out="channel.total_slides"/>
                </t>
                <small class="text-muted">Members</small>
                <small class="fw-medium text-end" t-out="channel.members_count"/>
            </div>
            <hr/>

            <!-- Share -->
            <button class="o_wslides_share btn btn-link w-100 text-decoration-none"
                    title="Share Channel" aria-label="Share Channel"
                    t-att-data-id="channel.id" t-att-data-name="channel.name"
                    t-att-data-url="channel.website_absolute_url" data-is-channel="True"
                    t-att-data-email-sharing="bool(channel.share_channel_template_id)">
                <i class="oi me-1" data-icon="family_history"/> Share
            </button>
        </div>
    </div>
</template>

<!-- Mobile: course details metadata shown below lesson list -->
<template id="course_metadata_mobile" name="Course Metadata - Mobile">
    <div class="d-lg-none mt-4 pt-4 border-top">
        <h6 class="fw-semibold mb-3">Course details</h6>
        <ul class="list-group list-group-flush">
            <li t-if="channel.user_id" class="list-group-item d-flex gap-3 justify-content-between px-0">
                <small class="text-muted">Responsible</small>
                <small class="fw-medium" t-out="channel.user_id.display_name"/>
            </li>
            <li class="list-group-item d-flex gap-3 justify-content-between px-0">
                <small class="text-muted">Last update</small>
                <small class="fw-medium" t-out="channel.slide_last_update" t-options="{'widget': 'date'}"/>
            </li>
            <li t-if="channel.total_time" class="list-group-item d-flex gap-3 justify-content-between px-0">
                <small class="text-muted">Duration</small>
                <small class="fw-medium" t-out="channel.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/>
            </li>
            <li t-if="channel.total_slides" class="list-group-item d-flex gap-3 justify-content-between px-0">
                <small class="text-muted">Lessons</small>
                <small class="fw-medium" t-out="channel.total_slides"/>
            </li>
            <li class="list-group-item d-flex gap-3 justify-content-between px-0">
                <small class="text-muted">Members</small>
                <small class="fw-medium" t-out="channel.members_count"/>
            </li>
        </ul>
    </div>
</template>

<!-- Mobile: context info blocks shown in main content column (prerequisite list and approval notice) -->
<template id="course_join_mobile_info" name="Course Join - Mobile Info Blocks">
    <div class="d-lg-none">
        <t t-if="not channel.is_member and not invite_preview and channel.prerequisite_channel_ids and not channel.prerequisite_user_has_completed">
            <!-- Prereq block — shown for both public and invite-only enroll -->
            <t t-set="_prereq_done" t-value="len(channel.prerequisite_channel_ids.filtered(lambda p: p.completion >= 100))"/>
            <div class="border rounded-3 p-3 mb-4">
                <div class="d-flex align-items-center justify-content-between mb-2">
                    <div class="d-flex align-items-center gap-2">
                        <span class="o_wslides_slide_icon o_wslides_slide_icon_sm d-flex align-items-center justify-content-center rounded bg-primary-subtle text-primary flex-shrink-0">
                            <i class="oi" data-icon="star"/>
                        </span>
                        <span class="fw-semibold">
                            <t t-if="channel.enroll == 'invite' and not channel.is_member_invited">Complete these first</t>
                            <t t-else="">Recommended first</t>
                        </span>
                    </div>
                    <small class="text-muted text-nowrap ms-2">
                        <t t-out="_prereq_done"/> of <t t-out="len(channel.prerequisite_channel_ids)"/> done
                    </small>
                </div>
                <p class="small text-muted mb-3">You can enroll now, but these courses will help you get the most out of it:</p>
                <div class="d-flex flex-column gap-2">
                    <t t-foreach="channel.prerequisite_channel_ids" t-as="prereq">
                        <a t-attf-href="/slides/#{prereq.id}"
                           class="d-flex align-items-center justify-content-between gap-2 p-2 px-3 rounded border text-decoration-none text-reset">
                            <div class="d-flex align-items-center gap-2 overflow-hidden">
                                <t t-if="prereq.completion >= 100">
                                    <span class="o_wslides_slide_icon o_wslides_slide_icon_sm d-flex align-items-center justify-content-center rounded bg-success-subtle text-success flex-shrink-0">
                                        <i class="oi" data-icon="check"/>
                                    </span>
                                </t>
                                <t t-elif="prereq.completion > 0">
                                    <span class="o_wslides_slide_icon o_wslides_slide_icon_sm d-flex align-items-center justify-content-center rounded bg-primary-subtle text-primary flex-shrink-0">
                                        <i class="oi" data-icon="play_arrow"/>
                                    </span>
                                </t>
                                <t t-else="">
                                    <span class="o_wslides_slide_icon o_wslides_slide_icon_sm d-flex align-items-center justify-content-center rounded bg-light flex-shrink-0"/>
                                </t>
                                <div class="overflow-hidden">
                                    <div class="fw-medium small text-truncate" t-out="prereq.name"/>
                                    <div class="text-muted smaller">
                                        <t t-if="prereq.completion >= 100">
                                            <span class="text-success"><i class="oi me-1" data-icon="check"/>Completed</span>
                                        </t>
                                        <t t-elif="prereq.completion > 0">
                                            <t t-if="prereq.total_slides"><t t-out="prereq.total_slides"/> lessons</t>
                                            <t t-if="prereq.total_time"> · <span t-out="prereq.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/></t>
                                            · <t t-out="prereq.completion"/>%
                                        </t>
                                        <t t-else="">
                                            Not started<t t-if="prereq.total_slides"> · <t t-out="prereq.total_slides"/> lessons</t><t t-if="prereq.total_time"> · <span t-out="prereq.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/></t>
                                        </t>
                                    </div>
                                </div>
                            </div>
                            <i class="oi flex-shrink-0" data-icon="arrow_right"/>
                        </a>
                    </t>
                </div>
            </div>
        </t>
        <t t-elif="not channel.is_member and not invite_preview and channel.enroll == 'invite' and not channel.is_member_invited and (not channel.prerequisite_channel_ids or channel.prerequisite_user_has_completed)">
            <!-- Approval required block (invite-only, prereqs done or none) -->
            <div class="border rounded-3 p-3 mb-4">
                <div class="d-flex align-items-center gap-2 mb-2">
                    <span class="o_wslides_slide_icon o_wslides_slide_icon_sm d-flex align-items-center justify-content-center rounded bg-primary-subtle text-primary flex-shrink-0">
                        <i class="oi" data-icon="mail"/>
                    </span>
                    <span class="fw-semibold">Approval required</span>
                </div>
                <div t-if="channel.enroll_msg" class="small text-muted" t-field="channel.enroll_msg"/>
                <p t-else="" class="small text-muted mb-0">This course requires approval to enroll. Request access to get started.</p>
            </div>
        </t>
    </div>
</template>

<!-- Mobile: fixed bottom CTA bar — handles all enrollment states -->
<template id="course_mobile_cta_bar" name="Course Mobile CTA Bar">
    <div class="o_wslides_mobile_cta_bar sticky-bottom d-flex d-lg-none align-items-center justify-content-between gap-3 p-3 bg-body border-top">
        <t t-if="channel.is_member">
            <!-- State 7: Completed -->
            <t t-if="channel.completed">
                <div class="min-w-0">
                    <span class="text-success"><i class="oi me-1" data-icon="check"/>Completed</span>
                    <small class="d-block text-muted">You've completed this course.</small>
                    <!-- Note: "Certificate available" subtitle requires website_slides_survey -->
                </div>
                <a t-attf-href="#{channel.website_url}?active_tab=review"
                   class="btn btn-primary flex-shrink-0">Review course</a>
            </t>
            <!-- State 6: In progress -->
            <t t-elif="channel.completion > 0">
                <t t-set="time_left" t-value="channel.total_time * (1 - channel.completion / 100.0) if channel.total_time else 0"/>
                <div class="d-flex flex-column gap-1 flex-grow-1 min-w-0">
                    <t t-call="website_slides.course_progress_bar"/>
                    <t t-call="website_slides.course_progress_stats_label"/>
                </div>
                <t t-set="_uncompleted" t-value="[s for s in channel.slide_content_ids if not channel_progress.get(s.id, {}).get('completed')]"/>
                <a t-att-href="_uncompleted[0].website_url if _uncompleted else channel.website_url"
                   class="btn btn-primary flex-shrink-0">Continue</a>
            </t>
            <!-- State 5: Enrolled, not started -->
            <t t-else="">
                <div class="min-w-0">
                    <div class="fw-semibold">Ready to begin</div>
                    <small class="text-muted">Not started yet</small>
                </div>
                <a t-att-href="channel.slide_content_ids[0].website_url if channel.slide_content_ids else channel.website_url"
                   class="btn btn-primary flex-shrink-0">Start course</a>
            </t>
        </t>
        <t t-elif="(invite_preview and channel.enroll in ['public', 'invite']) or (not channel.is_member and (channel.enroll == 'public' or (channel.is_member_invited and channel.enroll == 'invite')))">
            <!-- Non-member: public enroll or invited -->
            <t t-if="not invite_preview and channel.prerequisite_channel_ids and not channel.prerequisite_user_has_completed">
                <!-- State 1: Recommended first (prereqs not done, optional) -->
                <t t-set="_prereq_done" t-value="len(channel.prerequisite_channel_ids.filtered(lambda p: p.completion >= 100))"/>
                <div class="min-w-0">
                    <div class="fw-semibold">Recommended first</div>
                    <small class="text-muted"><t t-out="_prereq_done"/> of <t t-out="len(channel.prerequisite_channel_ids)"/> done · optional</small>
                </div>
                <a role="button" href="#"
                   class="btn btn-primary flex-shrink-0 o_wslides_js_course_join_link"
                   t-att-data-channel-id="channel.id"
                   t-att-data-channel-enroll="channel.enroll"
                   t-att-data-invite-hash="invite_hash"
                   t-att-data-invite-partner-id="invite_partner_id"
                   t-att-data-invite-preview="invite_preview"
                   t-att-data-is-member-or-invited="channel.is_member or channel.is_member_invited"
                   t-att-data-is-partner-without-user="is_partner_without_user">Enroll now</a>
            </t>
            <t t-else="">
                <!-- State 4: Free / simple join -->
                <div class="min-w-0">
                    <div class="fw-semibold">Free</div>
                    <!-- Note: "Lifetime access" is a design reference, no field available -->
                </div>
                <a role="button" href="#"
                   class="btn btn-primary flex-shrink-0 o_wslides_js_course_join_link"
                   t-att-data-channel-id="channel.id"
                   t-att-data-channel-enroll="channel.enroll"
                   t-att-data-invite-hash="invite_hash"
                   t-att-data-invite-partner-id="invite_partner_id"
                   t-att-data-invite-preview="invite_preview"
                   t-att-data-is-member-or-invited="channel.is_member or channel.is_member_invited"
                   t-att-data-is-partner-without-user="is_partner_without_user">Enroll now</a>
            </t>
        </t>
        <t t-elif="not invite_preview and channel.enroll == 'invite' and not channel.is_member and not channel.is_member_invited">
            <!-- State 2: Invite-only (approval required) -->
            <t t-if="channel.prerequisite_channel_ids and not channel.prerequisite_user_has_completed">
                <t t-set="_prereq_done" t-value="len(channel.prerequisite_channel_ids.filtered(lambda p: p.completion >= 100))"/>
                <div class="min-w-0">
                    <div class="fw-semibold">Complete first</div>
                    <small class="text-muted"><t t-out="_prereq_done"/> of <t t-out="len(channel.prerequisite_channel_ids)"/> done</small>
                </div>
            </t>
            <t t-else="">
                <div class="min-w-0">
                    <div class="fw-semibold">Approval required</div>
                    <t t-if="channel.has_requested_access">
                        <small class="text-success">Request already sent</small>
                    </t>
                </div>
            </t>
            <t t-if="is_public_user">
                <a t-attf-href="/web/login?redirect=/slides/#{channel.id}"
                   class="btn btn-primary flex-shrink-0">Sign in</a>
            </t>
            <t t-elif="not channel.has_requested_access">
                <a href="#" class="btn btn-primary flex-shrink-0 o_wslides_js_channel_enroll"
                   t-att-data-channel-id="channel.id">Request to enroll</a>
            </t>
        </t>
        <t t-elif="not invite_preview and not channel.is_member and channel.enroll == 'payment'">
            <!-- State 3: Paid course — uses product_info from website_sale_slides controller -->
            <t t-if="product_info and channel.product_id.website_published">
                <div class="min-w-0">
                    <div class="d-flex align-items-baseline gap-2 flex-wrap">
                        <span class="fw-semibold"
                              t-out="product_info['price']"
                              t-options="{'widget': 'monetary', 'display_currency': product_info['currency']}"/>
                        <del t-att-class="'text-muted small' + ('' if product_info['has_discounted_price'] else ' d-none')"
                             t-out="product_info['list_price']"
                             t-options="{'widget': 'monetary', 'display_currency': product_info['currency']}"/>
                    </div>
                </div>
                <div class="oe_website_sale">
                    <t t-set="product" t-value="channel.product_id"/>
                    <button name="add_to_cart"
                            class="btn btn-primary flex-shrink-0"
                            t-att-data-product-id="product.id"
                            t-att-data-product-template-id="product.product_tmpl_id.id"
                            t-att-data-product-type="product.product_tmpl_id.type"
                            t-att-data-show-quantity="False">Add to Cart</button>
                </div>
            </t>
            <t t-else="">
                <div class="text-muted small">Course Unavailable</div>
            </t>
        </t>
    </div>
</template>

<template id="course_join">
    <div class="o_wslides_js_course_join flex-grow-1 d-grid">
        <t t-if="(invite_preview and channel.enroll in ['public', 'invite']) or (not channel.is_member and (channel.enroll == 'public' or (channel.is_member_invited and channel.enroll == 'invite')))">
            <!-- Prerequisite course -->
            <div t-if="not invite_preview and channel.prerequisite_channel_ids and not channel.prerequisite_user_has_completed" class="mb-2">
                <h6>Recommended First</h6>
                <p class="small text-muted">You can enroll now, but these courses will help you get the most out of it:</p>
                <div class="d-flex flex-column gap-2 mb-3">
                    <t t-foreach="channel.prerequisite_channel_ids" t-as="prereq">
                        <a t-attf-href="/slides/#{prereq.id}"
                           class="d-flex align-items-center justify-content-between gap-2 p-2 px-3 rounded border text-decoration-none text-reset">
                            <div class="overflow-hidden">
                                <h6 class="mb-0 small" t-out="prereq.name"/>
                                <div class="text-muted smaller">
                                    <t t-if="prereq.completion &gt;= 100">
                                        <span class="text-success fw-medium">Completed</span>
                                    </t>
                                    <t t-elif="prereq.completion &gt; 0">
                                        <t t-if="prereq.total_slides"><t t-out="prereq.total_slides"/> lessons</t>
                                        <t t-if="prereq.total_time"> · <span t-out="prereq.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/></t>
                                        · <t t-out="prereq.completion"/>%
                                    </t>
                                    <t t-else="">
                                        Not started<t t-if="prereq.total_slides"> · <t t-out="prereq.total_slides"/> lessons</t><t t-if="prereq.total_time"> · <span t-out="prereq.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/></t>
                                    </t>
                                </div>
                            </div>
                            <i class="oi flex-shrink-0" data-icon="arrow_right"/>
                        </a>
                    </t>
                </div>
                <a role="button" class="o_wslides_js_course_join_link btn btn-primary d-block"
                    title="Start Course" aria-label="Start Course" href="#"
                    t-att-data-channel-id="channel.id"
                    t-att-data-channel-enroll="channel.enroll"
                    t-att-data-invite-hash="invite_hash"
                    t-att-data-invite-partner-id="invite_partner_id"
                    t-att-data-invite-preview="invite_preview"
                    t-att-data-is-member-or-invited="channel.is_member or channel.is_member_invited"
                    t-att-data-is-partner-without-user="is_partner_without_user">
                    Enroll anyway
                </a>
            </div>
            <div t-else="">
                <div class="d-flex align-items-center gap-1 mb-2">
                    <h4 class="mb-0 fw-bold">Free</h4>·<small class="text-muted">Lifetime access</small>
                </div>
                <a role="button" class="btn btn-primary o_wslides_js_course_join_link d-block"
                title="Start Course" aria-label="Start Course" href="#"
                t-att-data-channel-id="channel.id"
                t-att-data-channel-enroll="channel.enroll"
                t-att-data-invite-hash="invite_hash"
                t-att-data-invite-partner-id="invite_partner_id"
                t-att-data-invite-preview="invite_preview"
                t-att-data-is-member-or-invited="channel.is_member or channel.is_member_invited"
                t-att-data-is-partner-without-user="is_partner_without_user">
                    <span>Enroll now</span>
                </a>
            </div>
        </t>
        <!-- Invite only course -->
        <t t-elif="not invite_preview and channel.enroll == 'invite' and not channel.is_member and not channel.is_member_invited">
            <!-- Pre-requisite not done -->
            <div t-if="channel.prerequisite_channel_ids and not channel.prerequisite_user_has_completed" class="o_wslides_prerequisite_block">
                <h6>Complete these courses first:</h6>
                <p class="small text-muted">You can enroll now, but these courses will help you get the most out of it:</p>
                <div class="d-flex flex-column gap-2 mb-2">
                    <t t-foreach="channel.prerequisite_channel_ids" t-as="prereq">
                        <a t-attf-href="/slides/#{prereq.id}"
                           class="d-flex align-items-center justify-content-between gap-2 p-2 px-3 rounded border text-decoration-none text-reset">
                            <div class="overflow-hidden">
                                <h6 class="mb-0 small" t-out="prereq.name"/>
                                <div class="text-muted smaller">
                                    <t t-if="prereq.completion &gt;= 100">
                                        <span class="text-success fw-medium">Completed</span>
                                    </t>
                                    <t t-elif="prereq.completion &gt; 0">
                                        <t t-if="prereq.total_slides"><t t-out="prereq.total_slides"/> lessons</t>
                                        <t t-if="prereq.total_time"> · <span t-out="prereq.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/></t>
                                        · <t t-out="prereq.completion"/>%
                                    </t>
                                    <t t-else="">
                                        Not started<t t-if="prereq.total_slides"> · <t t-out="prereq.total_slides"/> lessons</t><t t-if="prereq.total_time"> · <span t-out="prereq.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/></t>
                                    </t>
                                </div>
                            </div>
                            <i class="oi flex-shrink-0" data-icon="arrow_right"/>
                        </a>
                    </t>
                </div>
                <t t-if="is_public_user">
                    <a t-att-href="'/web/login?redirect=/slides/%s' % (slug(channel))" class="btn btn-primary">Sign in</a>
                </t>
                <t t-else="">
                    <a href="#" class="o_wslides_js_channel_enroll btn btn-primary d-block" t-att-data-channel-id="channel.id">Request access</a>
                </t>
            </div>
            <!-- Pre-requisite done -->
            <div t-else="" t-attf-class="alert my-0 bg-100 p-2 #{'o_wslides_js_channel_enroll' if not is_public_user and channel.user_id else ''}"
                t-att-data-channel-id="channel.id">
                Private Course
                <div t-if="is_public_user">
                    <small>
                        Please <a t-att-href="'/web/login?redirect=/slides/%s' % (slug(channel))">sign in</a> to contact responsible
                    </small>
                </div>
                <div t-elif="channel.has_requested_access">
                    <small class="text-success">
                        Request already sent
                    </small>
                </div>
                <div t-else="" class="o_wslides_enroll_msg">
                    <small>
                        <div t-field="channel.enroll_msg"/>
                    </small>
                </div>
            </div>
        </t>
        <!-- Paid Course (xpath)-->
    </div>
    <hr/>
</template>

<template id="course_slides_list" name="Training Course content: list">
    <div class="mb-5 o_wslides_slides_list" t-att-data-channel-id="channel.id">
        <t t-set="chapter_num" t-value="0"/>
        <ul class="o_wslides_js_slides_list_container list-unstyled">
            <t t-set="j" t-value="0"/>
            <t t-foreach="category_data" t-as="category">
                <t t-set="category_id" t-value="category['id'] if category['id'] else None"/>
                <t t-if="category_id">
                    <t t-set="chapter_num" t-value="chapter_num + 1"/>
                </t>

                <li t-if="category['total_slides']" t-att-class="'o_wslides_slide_list_category o_wslides_js_list_item mb-2' if category_id else 'mt-1'" t-att-data-slide-id="category_id" t-att-data-category-id="category_id">
                    <div t-att-data-category-id="category_id"
                         class="o_wslides_slide_list_category_header position-relative d-flex justify-content-between align-items-center mb-3">
                        <t t-if="category_id">
                            <div class="d-flex align-items-center gap-3">
                                <t t-set="cat_done" t-value="sum(1 for s in category['slides'] if channel_progress.get(s.id, {}).get('completed'))"/>
                                <t t-set="category_done" t-value="cat_done &gt;= category['total_slides'] and category['total_slides'] &gt; 0"/>
                                <span t-attf-class="o_wslides_category_count rounded-circle text-center fw-semibold bg-primary-light text-primary-emphasis">
                                    <t t-out="chapter_num"/>
                                </span>
                                <h6 class="mb-0 h5" t-field="category['category'].name"/>
                            </div>
                            <div class="o_not_editable small">
                                <t t-if="channel.is_member or category_done">
                                    <span t-if="category_done" class="text-success"><i class="oi me-1" data-icon="check"/> Completed</span>
                                    <span t-else="" class="text-muted">
                                        <t t-out="cat_done"/> / <t t-out="category['total_slides']"/>
                                    </span>
                                </t>
                            </div>
                        </t>
                    </div>
                    <div class="list-group" t-att-data-category-id="category_id" t-attf-class="list-group border #{'' if category_last else 'mb-5'}">
                        <t t-foreach="category['slides']" t-as="slide">
                            <t t-call="website_slides.course_slides_list_slide" />
                            <t t-set="j" t-value="j+1"/>
                        </t>
                    </div>
                </li>
            </t>
        </ul>
        <t t-if="slide_count == 0" t-call="website_slides.course_slides_list_placeholder"/>
    </div>
    <div t-field="channel.description_html"/>
</template>

<template id="course_slides_list_placeholder" name="Course Placeholder Content">
    <div class="alert alert-light text-center" role="status">
        <t t-if="channel.can_publish">
            <strong>No lessons yet.</strong> Head over to the backend to create your first lesson.
        </t>
        <t t-else="">
            No lessons are available yet.
        </t>
    </div>
</template>

<template id="course_slides_list_sample" name="Course Sample Content">
    <ul class="list-unstyled mt-3" style="opacity: 50%;">
        <li class="o_wslides_slide_list_category mb-2">
            <div class="o_wslides_slide_list_category_header position-relative d-flex justify-content-between align-items-center mt8 bg-white shadow-sm border-bottom-0 py-2">
                <div class="d-flex align-items-center ps-3">
                    <span class="text-muted">Common tasks for a computer scientist</span>
                </div>
            </div>
            <ul class="list-unstyled pb-1 border-top card">
                <div class="h5 o_ribbon_right bg-primary text-uppercase">Sample</div>
                <li class="o_wslides_slides_list_slide bg-white-50 border-top-0 d-flex align-items-center ps-2 py-1 pe-2">
                    <i class="oi oi-filled py-2 mx-2" data-icon="article"/>
                    <div class="text-truncate me-auto">
                        <span>Asking Question</span>
                    </div>
                    <div class="d-flex flex-row">
                        <span class="badge fw-bold m-1 text-bg-warning">
                            <i class="oi oi-filled oi-fw" data-icon="flag"/> 10 xp
                        </span>
                    </div>
                </li>
                <li class="o_wslides_slides_list_slide bg-white-50 border-top-0 d-flex align-items-center ps-2 py-1 pe-2">
                    <i class="oi py-2 mx-2" data-icon="help"/>
                    <div class="text-truncate me-auto">
                        <span>Asking the right question</span>
                    </div>
                </li>
                <li class="o_wslides_slides_list_slide bg-white-50 border-top-0 d-flex align-items-center ps-2 py-1 pe-2">
                    <i class="oi py-2 mx-2" data-icon="picture_as_pdf"/>
                    <div class="text-truncate me-auto">
                        <span>Answering Questions</span>
                    </div>
                    <div class="d-flex flex-row">
                        <span class="badge text-bg-info badge-arrow-right fw-normal m-1">New</span>
                    </div>
                </li>
            </ul>
        </li>
        <li class="o_wslides_slide_list_category mb-2">
            <div class="o_wslides_slide_list_category_header position-relative d-flex justify-content-between align-items-center mt8 bg-white shadow-sm border-bottom-0 py-2">
                <div class="d-flex align-items-center ps-3">
                    <span class="text-muted">Parts of computer science</span>
                </div>
            </div>
            <ul class="list-unstyled pb-1 border-top card">
                <div class="h5 o_ribbon_right bg-primary text-uppercase">Sample</div>
                <li class="o_wslides_slides_list_slide bg-white-50 border-top-0 d-flex align-items-center ps-2 py-1 pe-2">
                    <i class="oi py-2 mx-2" data-icon="picture_as_pdf"/>
                    <div class="text-truncate me-auto">
                        <span>Mathematics</span>
                    </div>
                </li>
                <li class="o_wslides_slides_list_slide bg-white-50 border-top-0 d-flex align-items-center ps-2 py-1 pe-2">
                    <i class="oi py-2 mx-2" data-icon="picture_as_pdf"/>
                    <div class="text-truncate me-auto">
                        <span>Science</span>
                    </div>
                </li>
                <li class="o_wslides_slides_list_slide bg-white-50 border-top-0 d-flex align-items-center ps-2 py-1 pe-2">
                    <i class="oi py-2 mx-2" data-icon="play_arrow"/>
                    <div class="text-truncate me-auto">
                        <span>Logic</span>
                    </div>
                    <div class="d-flex flex-row">
                        <span class="badge text-bg-success fw-normal m-1">Preview</span>
                    </div>
                </li>
            </ul>
        </li>
    </ul>
</template>

<template id="course_slides_list_slide" name="Slide template for a training channel">
    <t t-set="can_link" t-value="not invite_preview and (slide.is_preview or channel.is_member)"/>
    <a t-att-index="j" t-att-data-slide-id="slide.id" t-att-data-category-id="category_id"
       t-att-href="'/slides/slide/%s' % slug(slide) if can_link else None"
       t-att-aria-disabled="None if can_link else 'true'"
       t-attf-class="list-group-item list-group-item-action o_wslides_slides_list_slide o_wslides_js_list_item d-flex align-items-center border-0 #{'o_wslides_js_slides_list_slide_link opacity-trigger-hover' if can_link else 'disabled'}">
        <span class="o_wslides_slide_icon flex-shrink-0 text-bg-light rounded me-3 d-flex align-items-center justify-content-center position-relative">
            <i t-attf-class="oi oi-fw #{'opacity-0-hover' if can_link else ''}" t-attf-data-icon="#{slide.slide_icon_class}"/>
            <i t-if="can_link" class="oi oi-fw position-absolute opacity-0 opacity-100-hover" data-icon="play_arrow"/>
            <span t-if="not can_link" class="position-absolute top-0 end-0 bg-body d-inline-flex me-n2 mt-n2 p-1 rounded-circle small" style="aspect-ratio: 1;">
                <i class="align-self-center oi text-muted" data-icon="lock"/>
            </span>
        </span>
        <div class="d-flex flex-column">
            <span t-field="slide.name"/>
            <small class="text-muted d-flex align-items-center gap-1">
                <span t-field="slide.slide_category"/>
                <t t-if="slide.completion_time">
                    <span>·</span>
                    <span t-field="slide.completion_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute', 'format': 'narrow'}"/>
                </t>
            </small>
        </div>

        <!-- Preview and new content badges -->
        <div class="d-flex align-items-center ms-auto">
            <span t-if="slide.is_new_slide and not channel_progress[slide.id].get('completed')" class="badge text-bg-info badge-arrow-right fw-normal m-1">
                New
            </span>
        </div>

        <!-- Completed circle-->
        <i t-if="channel_progress[slide.id].get('completed')" class="oi text-success" data-icon="check"/>
    </a>
</template>

<!-- ======= Documentation Course content: cards / categories=======  -->
<template id="course_promoted_slide" name="Documentation Course content: promoted slide">
    <div class="o_wslides_promoted_slide">
        <div t-if="not search and not search_slide_category and slide_promoted" class="container py-1 mb-2">
            <div class="card flex-column flex-lg-row">
                <a t-if="not invite_preview and (slide_promoted.is_preview or channel.is_member or is_slides_publisher)" t-attf-href="/slides/slide/#{slug(slide_promoted)}#{query_string}" class="w-100 w-lg-50 flex-shrink-0 rounded">
                    <div t-field="slide_promoted.image_1920" t-options="{'widget': 'image', 'style': 'height:100%', 'preview_image': 'image_512'}" class="h-100"/>
                </a>
                <div t-else="" class="w-100 w-lg-50 flex-shrink-0 rounded">
                    <div t-field="slide_promoted.channel_id.image_1920" t-options="{'widget': 'image', 'style': 'height:100%', 'preview_image': 'image_512'}" class="h-100"/>
                </div>

                <div class="card-body">
                    <a t-if="not invite_preview and (slide_promoted.is_preview or channel.is_member or is_slides_publisher)"
                    t-attf-href="/slides/slide/#{slug(slide_promoted)}#{query_string}"
                    class="h4 d-block pb-2 border-bottom" t-att-title="slide_promoted.name" t-field="slide_promoted.name"/>
                    <h4 t-else="" class="text-muted pb-2 border-bottom" t-field="slide_promoted.name"/>
                    <div class="o_line_clamp o_line_clamp_10 mt-3" t-field="slide_promoted.description"/>
                    <div t-if="slide_promoted.tag_ids" class="mt-2 pt-1">
                        <t t-foreach="slide_promoted.tag_ids" t-as="tag">
                            <h4 t-if="invite_preview" class="badge text-bg-info" t-out="tag.name"/>
                            <a t-else="" t-attf-href="/slides/#{slug(slide_promoted.channel_id)}/tag/#{slug(tag)}" class="badge text-bg-primary" t-out="tag.name"/>
                        </t>
                    </div>
                </div>
            </div>
        </div>
    </div>
</template>

<template id="course_slides_cards" name="Documentation Course content: cards / categories">
    <div t-if="not invite_preview" class="o_wslides_lesson_nav mb-4">
        <div class="container">
            <div class="row">
                <nav class="navbar navbar-expand-lg navbar-light bg-transparent col">
                    <a class="navbar-brand d-lg-none" href="#">Filter &amp; order</a>

                    <div class="ms-auto d-lg-none" t-if="search_slide_category or search">
                        <a t-att-href="'/slides/%s' % (slug(channel))" class="btn btn-info me-3">
                            <i class="oi me-1" data-icon="ink_eraser"/>Clear filters
                        </a>
                    </div>

                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse row" id="navbarSupportedContent">
                        <div class="col-12">
                            <ul class="navbar-nav me-lg-auto align-items-lg-center">

                                <t t-set="slide_category_keys" t-value="slide_categories.keys()"/>
                                <t t-foreach="slide_category_keys" t-as="slide_category_key">
                                    <t t-if="search_category">
                                        <li t-if="search_category['nbr_%s' % slide_category_key] > 0" class="nav-item">
                                            <a t-att-href="'/slides/%s/category/%s?%s' % (slug(channel), slug(search_category), keep_query(slide_category=slide_category_key))"
                                               t-att-class="'nav-link d-flex align-items-center justify-content-between me-1 %s' % ('active' if search_slide_category == slide_category_key else '')">
                                               <t t-out="slide_categories[slide_category_key]"/>
                                               <span t-attf-class="badge ms-1 #{'text-bg-info' if search_slide_category == slide_category_key else 'bg-400'}" t-out="search_category['nbr_%s' % slide_category_key]"/>
                                            </a>
                                        </li>
                                    </t>
                                    <t t-else="">
                                        <li t-if="channel['nbr_%s' % slide_category_key] > 0" class="nav-item">
                                            <a t-att-href="'/slides/%s?%s' % (slug(channel), keep_query(slide_category=slide_category_key))"
                                               t-att-class="'nav-link d-flex align-items-center justify-content-between me-1 %s' % ('active' if search_slide_category == slide_category_key else '')">
                                               <t t-out="slide_categories[slide_category_key]"/>
                                               <span t-attf-class="badge ms-1 #{'text-bg-info' if search_slide_category == slide_category_key else 'bg-400'}" t-out="channel['nbr_%s' % slide_category_key]"/>
                                            </a>
                                        </li>
                                    </t>
                                </t>
                            </ul>
                        </div>

                        <div class="col-12 d-flex align-items-start">
                            <ul class="navbar-nav me-auto">
                                <li class="nav-item dropdown ms-lg-auto">
                                    <a class="nav-link dropdown-toggle dropdown-toggle align-items-center d-flex" type="button" id="slidesChannelDropdownSort"
                                       data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false" href="#">
                                        <b>Order by</b>
                                        <span class="d-none d-xl-inline">:
                                            <t t-if="sorting == 'most_voted'">Most Voted</t>
                                            <t t-elif="sorting == 'most_viewed'">Most Viewed</t>
                                            <t t-else="">Newest</t>
                                        </span>
                                    </a>
                                    <div class="dropdown-menu" aria-labelledby="slidesChannelDropdownSort" role="menu">
                                        <h6 class="dropdown-header">Sort by</h6>
                                        <a role="menuitem" t-att-href="'/slides/%s?%s' % (slug(channel), keep_query('slide_category', sorting='latest'))"
                                           t-att-class="'dropdown-item %s' % ('active' if sorting and sorting == 'latest' else '')">Newest</a>
                                        <a role="menuitem" t-att-href="'/slides/%s?%s' % (slug(channel), keep_query('slide_category', sorting='most_voted'))"
                                           t-att-class="'dropdown-item %s' % ('active' if sorting and sorting == 'most_voted' else '')">Most Voted</a>
                                        <a role="menuitem" t-att-href="'/slides/%s?%s' % (slug(channel), keep_query('slide_category', sorting='most_viewed'))"
                                           t-att-class="'dropdown-item %s' % ('active' if sorting and sorting == 'most_viewed' else '')">Most Viewed</a>
                                    </div>
                                </li>
                            </ul>

                            <div class="me-3 d-none d-lg-inline-block">
                                <a t-if="search_slide_category or search" t-att-href="'/slides/%s' % (slug(channel))" class="btn btn-sm btn-info ms-1">
                                    <i class="oi me-1" data-icon="ink_eraser"/>Clear filters
                                </a>
                            </div>

                            <form t-attf-action="/slides/#{slug(channel)}" role="search" method="get" class="my-2 my-lg-0">
                                <div class="input-group position-relative">
                                    <input type="text" class="form-control border" name="search" placeholder="Search in content" t-att-value="search"/>
                                    <button class="btn border" type="submit" aria-label="Search" title="Search">
                                        <i class="oi" data-icon="search"/>
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>

    <div class="container">
        <div class="row">
            <div class="mb-2 pt-1 text-start col">
                <t t-if="channel_frontend_tags">
                    <t t-foreach="channel_frontend_tags" t-as="channel_tag">
                        <span t-attf-class="badge o_wslides_channel_tag #{'o_color_'+str(channel_tag.color)}" t-out="channel_tag.name"/>
                    </t>
                </t>
                <a t-if="channel.can_upload"
                    class="o_wslides_js_channel_tag_add badge text-bg-primary fw-normal m-1"
                    role="button"
                    aria-label="Add Tag"
                    href="#"
                    t-att-data-channel-id="channel.id"
                    t-att-data-channel-tag-ids="channel.tag_ids.ids">
                    <span>Add Tag</span>
                </a>
            </div>

            <div t-if="channel.can_upload" class="text-end pb-2 col-auto">
                <a class="btn btn-primary py-1 o_wslides_js_slide_upload"
                    title="Upload Document" role="button"
                    href="#"
                    t-att-data-channel-id="channel.id"
                    t-att-data-can-upload="channel.can_upload"
                    t-att-data-can-publish="channel.can_publish">
                    <i class="oi me-1" data-icon="cloud_upload"/>Add Content
                </a>
                <a class="btn btn-secondary py-1 o_wslides_js_slide_section_add"
                    title="Add Section" role="button"
                    href="#"
                    t-att-channel_id="channel.id">
                    <i class="oi me-1" data-icon="folder"/>Add a section
                </a>
            </div>
        </div>
    </div>
    <!-- Featured lesson  -->
    <t t-if="channel.promote_strategy != 'none'">
        <t t-call="website_slides.course_promoted_slide"/>
    </t>

    <div class="container py-2">
        <t t-if="search">
            <t t-set="search_results_number" t-value="0"/>
            <t t-foreach="category_data" t-as="category">
                <t t-set="search_results_number" t-value="search_results_number + category['total_slides']"/>
            </t>
            <div t-if="search_results_number == 0" class="alert alert-info mt-4 mb-5 text-center">
                No content was found using your search <span class="fw-bold" t-out="search"/>.
            </div>
        </t>

        <t t-foreach="category_data" t-as="category">
            <div class="mb-2" t-if="(category['slides'] or channel.can_publish) and (search_category and search_category.id == category['id'] or not search_category)">
                <t t-set="is_empty_editable" t-value="not category['slides'] and channel.can_publish"/>
                <div class="d-flex align-items-center justify-content-between border-bottom pb-2 mb-3" t-if="category['id'] and query_string != '?search_uncategorized=1'">
                    <h5 t-attf-class="m-0 #{'text-muted' if is_empty_editable else ''}"><t t-out="category['name']"/></h5>
                    <a t-if="category['id'] and not is_empty_editable" t-attf-href="/slides/#{channel.id}/category/#{category['id']}?#{keep_query('invite_hash', 'invite_partner_id') if invite_preview else ''}">View all</a>
                </div>
                <div class="d-flex align-items-center justify-content-between border-bottom pb-2 mb-3" t-if="not category['id'] and len(category['slides']) > 0">
                    <h5 t-if="len(category_data) > 1" t-attf-class="m-0 #{'text-muted' if is_empty_editable else ''}"><t t-out="category['name']"/></h5>
                    <a t-attf-href="/slides/#{channel.id}?uncategorized=1&amp;#{keep_query('invite_hash', 'invite_partner_id') if invite_preview else ''}">View all</a>
                </div>
                <div class="row mx-n2">
                    <t t-foreach="category['slides']" t-as="slide">
                        <div class="col-12 col-sm-6 col-lg-4 px-2 d-flex"><t t-call="website_slides.lesson_card"/></div>
                    </t>
                </div>
            </div>
        </t>

        <div class="row">
            <div class="col" t-field="channel.description_html"/>
        </div>
    </div>
    <t t-if="search_category or search_uncategorized">
        <div class="d-flex justify-content-center pb-5">
            <t t-call="website.pager"/>
        </div>
    </t>
</template>

<template id='lesson_card' name="Lesson Card">
    <div class="card w-100 o_wslides_lesson_card mb-4" t-att-data-publish="slide.website_published and 'on' or 'off'">
        <t t-if="slide.is_new_slide and not channel_progress[slide.id].get('completed')" t-call="website_slides.course_card_information"/>
        <t t-set="can_access" t-value="not invite_preview and (slide.is_preview or channel.is_member or channel.can_publish)"/>
        <t t-if="not slide.is_published and channel.can_publish" t-call="website.unpublished_badge"/>
        <a t-if="can_access" t-attf-href="/slides/slide/#{slug(slide)}#{query_string}" t-title="slide.name" style="height:150px">
            <div t-field="slide.image_1920" t-options="{'widget': 'image', 'preview_image': 'image_512'}" class="o_wslides_background_image h-100"/>
        </a>
        <div t-else="" class="o_wslides_background_image" style="height:150px">
            <div t-field="slide.channel_id.image_1920" t-options="{'widget': 'image', 'preview_image': 'image_512'}" class="o_wslides_background_image h-100"/>
        </div>
        <i t-if="channel_progress[slide.id].get('completed')" class="oi oi-filled position-absolute py-1 px-2 h5 text-primary" data-icon="check_circle" style="right:0; top:0;"/>

        <div class="card-body d-flex flex-column px-3">
            <a t-if="can_access" class="card-title h5 o_line_clamp" t-attf-href="/slides/slide/#{slug(slide)}#{query_string}" t-out="slide.name"/>
            <span t-else="" class="card-title h5 o_line_clamp text-muted" t-out="slide.name"/>
            <div class="text-muted o_line_clamp mb-2" t-if="slide.is_preview or (not slide.is_published and user.has_group('website_slides.group_website_slides_officer'))">
                <span t-if="slide.is_preview" class="badge text-bg-info">Preview</span>
            </div>
            <div class="card-text mb-auto">
                <div class="o_line_clamp o_line_clamp_3 fw-light oe_no_empty" t-field="slide.description"/>
            </div>
            <div class="text-muted o_line_clamp my-2">
                <t t-foreach="slide.tag_ids" t-as="tag">
                    <h4 t-if="invite_preview" class="badge text-bg-info" t-out="tag.name"/>
                    <a t-else="" t-attf-href="/slides/#{slug(slide.channel_id)}/tag/#{slug(tag)}" class="badge text-bg-primary" t-out="tag.name"/>
                </t>
            </div>
            <span t-if="channel.is_member and channel_progress[slide.id].get('completed')" class="badge text-bg-success align-self-start"><i class="oi me-1" data-icon="check"/>Completed</span>
        </div>
        <div class="card-footer bg-white text-600">
            <div class="d-flex align-items-center small">
                <span class="fw-bold me-auto" t-field="slide.completion_time" t-options='{"widget": "float_time"}'/>
                <div class="o_wslides_js_slide_like">
                    <span t-attf-class="d-inline-block text-center o_wslides_js_slide_like_up #{'disabled' if not channel.can_vote else ''}" tabindex="0" data-bs-toggle="popover" t-att-data-slide-id="slide.id" t-att-data-user-vote="slide.user_vote">
                        <i t-attf-class="oi oi-1x #{'oi-filled' if slide.user_vote == 1 else ''}" data-icon="thumb_up" role="img" aria-label="Likes" title="Like"></i>
                        <span t-field="slide.likes" t-options="{'format_decimalized_number': True}"/>
                    </span>
                    <span t-attf-class="d-inline-block text-center ms-1 o_wslides_js_slide_like_down #{'disabled' if not channel.can_vote else ''}" tabindex="0" data-bs-toggle="popover" t-att-data-slide-id="slide.id" t-att-data-user-vote="slide.user_vote">
                        <i t-attf-class="oi oi-1x #{'oi-filled' if slide.user_vote == -1 else ''}" data-icon="thumb_down" role="img" aria-label="Dislikes" title="Dislike"></i>
                        <span t-field="slide.dislikes" t-options="{'format_decimalized_number': True}"/>
                    </span>
                </div>
            </div>
        </div>
    </div>
</template>

</data></odoo>
