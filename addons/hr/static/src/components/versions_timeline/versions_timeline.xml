<?xml version="1.0"?>
<templates xml:space="preserve">

    <t t-name="hr.VersionsTimeline" t-inherit="web.StatusBarField" t-inherit-mode="primary">
        <div t-custom-ref="root" position="after">
            <div class="o_arrow_button_wrapper">
                <button
                    t-custom-ref="datetime-picker-target-version"
                    title="New Employee Record"
                    t-on-click="this.onClickDateTimePickerBtn"
                    class="btn btn-primary">
                    <i class="fa fa-plus"/>
                </button>
            </div>
        </div>

        <button t-custom-ref="after" position="replace">
            <div class="o_arrow_button_wrapper">
                <button
                    t-custom-ref="after"
                    type="button"
                    class="btn btn-secondary dropdown-toggle o_arrow_button o_first"
                    t-att-disabled="this.props.isDisabled"
                    aria-label="More..."
                >
                    ...
                </button>
            </div>
        </button>

        <button t-custom-ref="before" position="replace">
            <div class="o_arrow_button_wrapper">
                <button
                    t-custom-ref="before"
                    type="button"
                    class="btn btn-secondary dropdown-toggle o_arrow_button o_last"
                    t-att-disabled="this.props.isDisabled"
                    aria-label="More..."
                >
                    ...
                </button>
            </div>
        </button>

        <t t-foreach="this.items.inline" position="replace">
            <t t-foreach="this.items.inline" t-as="item" t-key="item.value">
            <div class="o_arrow_button_wrapper position-relative">
                <button
                    type="button"
                    class="btn btn-secondary o_arrow_button"
                    t-att-class="{
                        o_first: item_first,
                        o_arrow_button_current: item.isSelected,
                        o_last: item_last,
                    }"
                    t-att-disabled="this.props.isDisabled || item.isSelected"
                    role="radio"
                    t-att-aria-checked="item.isSelected.toString()"
                    t-att-aria-current="item.isSelected and 'step'"
                    t-att-data-value="item.value"
                    t-on-click="() => this.selectItem(item)"
                    t-att-data-tooltip="item.toolTip">
                        <t t-out="item.label"/>
                    </button>
                    <span
                            t-if="this.displayContractLines"
                            class="o_contract_line position-absolute top-100 start-0 end-0 d-block"
                            t-att-class="{
                                'border-top border-primary': item.isInContract and item.isCurrentContract,
                                'me-1': item_first,
                                'ms-1': item_last,
                            }"/>
                </div>
            </t>
        </t>
    </t>
</templates>
