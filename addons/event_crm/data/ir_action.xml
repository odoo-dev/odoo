<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="action_enrich_mail" model="ir.actions.server" >
            <field name="name">Generate Leads</field>
            <field name="model_id" ref="event.model_event_event"/>
            <field name="groups_id" eval="[Command.link(ref('event.group_event_manager'))]"/>
            <field name="state">code</field>
            <field name="code">
if record:
    action = record.action_generate_leads()
            </field>
        </record>

    </data>
</odoo>
