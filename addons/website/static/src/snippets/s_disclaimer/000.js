import publicWidget from "@web/legacy/js/public/public_widget";

publicWidget.registry.Disclaimer = publicWidget.Widget.extend({
    selector: ".s_disclaimer",
    events: {},

    /**
     * @override
     */
    async start() {
        // TODO
        return this._super(...arguments);
    }
});