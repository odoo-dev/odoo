<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

<t t-name="website.GalleryElementOption">
    <BuilderRow label.translate="Re-order" disabled="!state.hasMultiItems">
        <BuilderButtonGroup preview="false" action="'setGalleryElementPosition'">
            <BuilderButton icon="'keyboard_double_arrow_left'" title.translate="Move to first"  actionValue="'first'" className="!state.hasMultiItems || state.isFirstItem ? 'disabled': ''"/>
            <BuilderButton icon="'keyboard_arrow_left'" title.translate="Move to previous" actionValue="'prev'" className="!state.hasMultiItems ? 'disabled': ''"/>
            <BuilderButton icon="'keyboard_arrow_right'" title.translate="Move to next" actionValue="'next'" className="!state.hasMultiItems ? 'disabled': ''"/>
            <BuilderButton icon="'keyboard_double_arrow_right'" title.translate="Move to last" actionValue="'last'" className="!state.hasMultiItems || state.isLastItem ? 'disabled': ''"/>
        </BuilderButtonGroup>
    </BuilderRow>
</t>

</templates>
