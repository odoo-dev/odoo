<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_company_form" model="ir.ui.view">
        <field name="name">res.company.form.stock.account</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="account.view_company_form"/>
        <field name="arch" type="xml">
            <field name="currency_id" position="after">
                <field name="cost_method"/>
            </field>
        </field>
    </record>
    <record model="ir.actions.server" id="action_closing">
        <field name="name">Closing</field>
        <field name="model_id" ref="model_res_company"/>
        <field name="binding_model_id" ref="model_res_company" />
        <field name="state">code</field>
        <field name="code">
action = records.post_stock_valuation(periodic_cogs=True)
        </field>
    </record>
</odoo>
