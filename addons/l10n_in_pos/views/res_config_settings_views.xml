<?xml version="1.0"?>
<odoo>
    <record id="pos_config_view_form_inherit_l10n_in_pos" model="ir.ui.view">
        <field name="name">pos.config.form.view.inherit.l10n_in_pos</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='payment_methods_new']" position="before">
                <div class="o_notification_alert alert alert-warning" role="alert" invisible="not is_kiosk or country_code != 'IN'">
                    <span>Please note that the kiosk for INR currency only works with Razorpay terminal</span>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
