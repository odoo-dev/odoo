<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="variant_image_assignment">
        <Dropdown
            t-if="showDropdown"
            state="dropdownState"
            beforeOpen.bind="beforeOpen"
        >
            <button
                t-attf-class="btn btn-outline-secondary border-top-0 rounded-0 w-100 #{this.record.context.default_product_variant_ids &amp;&amp; !this.record.data.product_variant_ids.resIds.includes(this.record.context.default_product_variant_ids?.[0]) ? 'opacity-50' : ''}"
            >
                <div class="d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center gap-2">
                        <span>Apply to attributes</span>
                        <span class="badge rounded-pill bg-secondary">
                            <t t-if="selectedCount" t-out="selectedCount"/>
                            <t t-else="">All</t>
                        </span>
                    </div>
                    <div class="d-flex align-items-center gap-1">
                        <span
                            t-if="record.data.is_primary_or_secondary and record._parentRecord.resModel === 'product.template'"
                            class="badge rounded-1 bg-warning-subtle text-warning-emphasis"
                        >
                            <t t-out="record.data.is_primary_or_secondary.charAt(0).toUpperCase() + record.data.is_primary_or_secondary.slice(1)"/>
                        </span>
                        <i t-if="!dropdownState.isOpen" class="fa fa-caret-down ms-1"/>
                        <i t-else="" class="fa fa-caret-up ms-1"/>
                    </div>
                </div>
            </button>
            <t t-set-slot="content">
                <t
                    t-if="state.attributes.length"
                    t-foreach="state.attributes"
                    t-as="attr"
                    t-key="attr.id"
                >
                    <t t-foreach="attr.values" t-as="val" t-key="val.id">
                        <DropdownItem
                            class="'d-flex align-items-center justify-content-between gap-3 px-3'"
                            onSelected="() => this.toggleValue(val.id)"
                            closingMode="'none'"
                        >
                            <div class="o-checkbox form-check">
                                <input
                                    class="form-check-input"
                                    type="checkbox"
                                    t-att-checked="state.checkedIds.has(val.id)"
                                />
                                <span class="small">
                                    <t t-out="val.name"/>
                                </span>
                            </div>
                        </DropdownItem>
                    </t>
                    <div t-if="!attr_last" class="d-flex align-items-center my-1 px-2">
                        <span class="mx-2 small text-muted">And</span>
                        <div class="flex-grow-1 border-top"/>
                    </div>
                </t>
            </t>
        </Dropdown>
    </t>

</templates>
