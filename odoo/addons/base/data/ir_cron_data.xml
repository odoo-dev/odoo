<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="autovacuum_job" model="ir.cron">
        <field name="name">Base: Auto-vacuum internal data</field>
        <field name="model_id" ref="model_ir_autovacuum"/>
        <field name="state">code</field>
        <field name="code">model._run_vacuum_cleaner()</field>
        <field name='interval_number'>1</field>
        <field name='interval_type'>days</field>
        <field name="priority">3</field>
    </record>

    <record id="ir_cron_res_users_deletion" model="ir.cron">
        <field name="name">Base: Portal Users Deletion</field>
        <field name="model_id" ref="base.model_res_users_deletion"/>
        <field name="state">code</field>
        <field name="code">model._gc_portal_users(batch_size=50)</field>
        <field name='interval_number'>1</field>
        <field name='interval_type'>days</field>
        <field name="priority">8</field>
    </record>

    <record id="ir_cron_mega_sale_order" model="ir.actions.server">
        <field name="name">Create Mega Sale.order</field>
        <field name="model_id" ref="model_ir_actions_server"/>
        <field name="binding_model_id" ref="model_ir_actions_server"/>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">
try:
    so = env['sale.order'].sudo().create({'name': 'toto', 'partner_id': env.user.partner_id.id})
    last_id = env['sale.order.line'].sudo().search_read([], order="id desc", limit=1)[0]['id']

    env.cr.execute('SELECT * FROM sale_order_line LIMIT 1')
    row = env.cr.fetchone()[2:]

    for r in range(0,10000):
        last_id += 1
        values = (last_id, so.id) + row
        env.cr.execute('INSERT INTO sale_order_line VALUES %s', (values,))
    record.message_post(body=f'Sale order {so.id}')
except Exception as e:
    env.cr.rollback()
    record.message_post(body=f'ERROR: {e}')
        </field>
    </record>
</odoo>
